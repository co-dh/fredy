# S1_62 sorry status (§1.62–§1.635)

This file has 5 `sorry`-bearing declarations. The task targeted the two named §1.633 / §1.635
sorries; both are **genuine blockers**. Details below, plus the state of the other three for context.

## The two targeted sorries — BLOCKED

### §1.635 `prelogos_representation_theorem` (line ~219)
The book's faithful representation of a small positive pre-logos in a power of Set chains four
pieces, none of which exist in this S1 development:

1. **Capital extension (§1.63)** — embed into a capital positive pre-logos. Depends on §1.633
   (`capital_iff_complemented_subterminators`), itself blocked (below).
2. **Boolean algebra of complemented subterminators** — needs §1.631 projectivity, blocked on
   coproduct **disjointness** (`HasBinaryCoproducts` here carries no disjoint/universal axioms;
   the S1_64 note at lines 103–111 states exactly this gap).
3. **Stone representation via ultra-filters** — ultra-filter existence is a **choice** principle,
   forbidden by project policy (CLAUDE.md) unless unavoidable; no constructive substitute exists.
4. **T_ℱ construction (§1.634)** — needs the colimit of Hom-sets. `prefilter_functor` here is only
   the raw pair-type `PrefilterMap`, not the colimit, and nothing proves it preserves the
   pre-logos structure.

Closing this faithfully would require building all four (and accepting choice for #3). Not done.

### §1.633 `capital_iff_complemented_subterminators` (line ~295)
Deep iff; both directions blocked:

- **(⟹)** "complemented subterminators projective" is precisely §1.631
  (`complemented_of_projective_is_projective`), which is blocked on coproduct disjointness (its
  inner `hh : Cover h` needs disjoint coproduct inclusions). The "form a basis" conjunct needs the
  §1.632 basis theory (only `IsBasis` is *defined*; no constructions produce instances).
- **(⟸)** reconstructing Capital (§1.523/§1.525 well-pointedness) from such a basis requires turning
  a basis arrow `G → A` (G a subterminator) into a point `1 → A` — i.e. subterminators as
  well-supported quotients of 1 — which is unbuilt.

Both docstrings in `S1_62.lean` were sharpened to record these precise blockers.

## Other sorries in the file (not targeted, all blocked on the same root cause)

- **§1.62 `pasting_lemma`** (line ~119) — the book proves it with allegory relations
  (`R = x°f ∪ y°g`, entire + simple ⟹ map). That machinery lives in `S2_*` (`Entire`, `Simple`,
  `Map`, `Allegory`) and is **not importable** here without circularity. Blocked.
- **§1.624 `decompose_via_coproduct`** (line ~150) — depends directly on `pasting_lemma`. Blocked.
- **§1.631 `complemented_of_projective_is_projective`** (lines ~260, ~284) — two inner sorries,
  both needing coproduct **disjointness** (`Cover h` for `h = case (y≫inl) inr`, and the
  factorization of `σ` through the B-summand). The coproduct here has no disjointness axiom.

## Root cause summary
The single dominating blocker is that `HasBinaryCoproducts` in S1_58 is a *bare* coproduct with no
**disjoint / universal** axioms, plus the allegory relation calculus (needed for `pasting_lemma`)
living only in the S2 files. Until disjoint coproducts (§1.63) and the relation calculus are
available in the S1 layer — and a constructive replacement for the ultra-filter theorem is found —
none of these five declarations can be closed without faking the statement.

No sorries were closed; statements left verbatim, docstrings sharpened. Build remains green.
