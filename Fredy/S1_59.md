# §1.59 Abelian Categories — Formalization Tracker

## Status Summary

| Section | Statement                                              | Type      | Status  | Decl / Note                                          |
|---------|--------------------------------------------------------|-----------|---------|------------------------------------------------------|
| §1.591  | Zero object 0≅1; zero morphisms form two-sided ideal   | def+thm   | DONE    | `HasZeroObject`, `zeroMorphism`, `zero_morphism_comp`|
| §1.592  | Kernel = eqz(x,0); Cokernel = coeqz(x,0)             | def       | DONE    | `Kernel`, `kernelMap`, `Cokernel`, `cokernelMap`     |
| §1.593  | Normal subobject = kernel of something                 | def       | DONE    | `IsNormalSubobject`                                  |
| §1.593  | Abelian ↔ regular additive + all-normal                | thm       | SORRY   | `abelian_iff_regular_additive_all_normal`            |
| §1.593  | Abelian category class                                 | class     | DONE    | `AbelianCategory`                                    |
| §1.594  | Effective regular additive → all-normal → abelian      | thm       | SORRY   | `effective_regular_additive_is_abelian`              |
| §1.595  | Abelian group object in a category with products       | structure | DONE    | `AbelianGroupObject`, `IsHomAbelianGroupObject`      |
| §1.595  | Hom-set in Ab(A)                                       | def       | DONE    | `HomAb`                                              |
| §1.595  | Ab(A) is abelian for effective regular A               | thm       | MISSING | Cat instance for Ab(A) not yet built                 |
| §1.597  | Exact category definition                              | class     | DONE    | `ExactCategory`                                      |
| §1.597  | Monic is kernel of its own cokernel (in exact cat)     | thm       | DONE    | `monic_kernel_of_cokernel` (no axioms)               |
| §1.597  | Abelian ↔ exact additive (+ binary products)           | thm       | SORRY   | `abelian_iff_exact_additive`                         |
| §1.598  | Left-normal, right-normal, normal category             | def       | DONE    | `IsLeftNormal`, `IsRightNormal`, `IsNormalCategory`  |
| §1.598  | Abelian ↔ normal + kernels + cokernels + products      | thm       | SORRY   | `abelian_iff_normal_kernels_cokernels`               |
| §1.599  | Five lemma                                             | thm       | SORRY   | `five_lemma`                                         |
| §1.599  | Snake lemma (connecting morphism + exact sequence)     | thm       | SORRY   | `snake_lemma`                                        |

Note: `ExactAt` (exact at B = image f ≅ kernel g) lives in `S1_39.lean` which imports `S1_59.lean`.
The five lemma and snake lemma use `Isomorphic (image _).dom (Kernel _)` directly.

## MISSING

- **§1.595 (Ab(A) abelian)**: Requires a `Cat` instance for `AbelianGroupObject 𝒞` as a
  category (composition, identity, etc.). This needs universe-polymorphic hom-set
  infrastructure. Blocked pending that infrastructure.
- **§1.596**: (Ab(Sh(Y)) = sheaves of abelian groups) — not attempted; requires sheaf theory.

## Sorries and Why

| Decl                                         | Why sorry                                                                                                  |
|----------------------------------------------|------------------------------------------------------------------------------------------------------------|
| `effective_regular_additive_is_abelian`      | Requires formalizing the Ab-calculus (§1.55) and inverse-image lemma (§1.582); deep dependency.          |
| `abelian_iff_regular_additive_all_normal`    | TYPECLASS-COHERENCE BLOCKER (not a calculus gap). See note below.                                         |
| `abelian_iff_exact_additive`                 | Proof in §1.597 spans ~2 pages; subtraction construction, regularity, normality all need formalization.   |
| `abelian_iff_normal_kernels_cokernels`       | Proof in §1.598 constructs equalizers and pullbacks from normal hypotheses; multi-step.                   |
| `five_lemma`                                 | Goal is bare `IsIso v₃`; needs ker(v₃)=0 ∧ coker(v₃)=0 via the §1.543 Horn-sentence reduction to Ab (or §1.55 element chase). No shortcut from `AbelianCategory`/`ExactCategory` fields. §1.543 owned by concurrent agent, not importable. |
| `snake_lemma`                                | δ is built "as a relation" (§1.599) needing §1.55+§1.56 relational calculus; exactness reduces to Ab via §1.543 capitalization. κ_*/π_* alone are not the hard core; partial proof would still `sorry` δ inside the existential.     |

## Sharpened blocker: `abelian_iff_regular_additive_all_normal`

This is NOT (as previously noted) a "cokernels-from-normality" calculus gap — `HasCoequalizers`
is already a hypothesis.  The true blocker is a **typeclass-coherence diamond** in the Lean encoding:

- `AbelianCategory` bundles `HasZeroObject`, `HasBinaryProducts`, `HasTerminal`, … as *fields*.
- The theorem's hypotheses supply `RegularCategory`, `AdditiveCategory`, `HasZeroObject`, … as
  *independent* instances whose shared parents (`HasTerminal`, `HasBinaryProducts`, …) are **not
  defeq**.  E.g. `reg.toHasTerminal ≠ zo.toHasTerminal`, `add.toHasBinaryProducts ≠ reg.toHasBinaryProducts`.
- Building `AbelianCategory` forces ONE coherent choice of terminal/products/coterminal; mixing the
  ambient instances triggers "field already specified" / `Type mismatch` on `prod_coprod_coincide`
  and `zero_eq_one`.
- The same mismatch breaks even the *trivial* (⟸) direction: `ab.all_normal` produces
  `IsNormalSubobject` w.r.t. the abelian category's *own* zero/equalizer instances, which differ
  from the ambient `[HasZeroObject]`/`[HasEqualizers]` appearing in the goal.

FIX OPTIONS (future work):
1. Reformulate the theorem with a single ambient bundle (e.g. take `[BicartesianCategory 𝒞]` or a
   pre-bundled "candidate-abelian" structure) so all parents are shared by construction.
2. Add propositional-coherence lemmas (`Subsingleton (HasTerminal 𝒞)`, etc.) and transport across them.
   Freyd never faces this because his categories carry a single ambient structure; the gap is purely
   an artifact of independent typeclass instances.

## `ExactCategory` strengthened (§1.597 faithfulness)

`ExactCategory.exact` now carries the canonical *factorization equation*
`cokernelMap (kernelMap x) ≫ θ ≫ kernelMap (cokernelMap x) = x` alongside `IsIso θ`.
Freyd §1.597 defines exactness via *this specific* coimage→image map being an iso, so the
equation is part of the data, not an afterthought.  Without it `monic_kernel_of_cokernel` is
unprovable (θ would be an *arbitrary* iso unrelated to x).  `ExactCategory` is used only in S1_59.

## Build

`lake build Fredy.S1_59` — green (0 errors; 6 faithful sorries remain, warnings only).
`monic_kernel_of_cokernel` and `zeroMorphism_comp_left` verified axiom-free (`lean_verify`).

## Discharge audit (2026-06-16)

All 6 sorries re-examined against current additive/exact infrastructure (S1_52 regular,
S1_56 reciprocation/covers, S1_58 bicartesian/`HalfAdditiveCategory`+`AdditiveCategory`).
None is dischargeable honestly — each is a true, non-vacuous Freyd §1.59 statement blocked on
absent infrastructure, NOT a false/fakeable goal:

- The two iffs that construct an `AbelianCategory` (`abelian_iff_regular_additive_all_normal`,
  `abelian_iff_exact_additive`, `abelian_iff_normal_kernels_cokernels`) need the cokernel /
  subtraction / pullback constructions Freyd does over ~2 pages each.
- `effective_regular_additive_is_abelian` needs the §1.55 Ab-calculus + §1.582 inverse-image.
- `five_lemma`, `snake_lemma` need the §1.543 capitalization Horn-sentence reduction to Ab
  (owned by a concurrent agent, not importable) and/or §1.55/§1.56 relational element-chasing.

Verified the diamond blocker is genuine via `lean_multi_attempt`: even the trivial (⟸) of
`abelian_iff_regular_additive_all_normal` fails because `ab.all_normal` yields
`IsNormalSubobject` w.r.t. `ab.toHasEqualizers`/`ab.toHasTerminal`, not the ambient instances
(non-defeq; not a `Subsingleton`, so transport-fix option 2 in the blocker note does not apply).
In-file blocker comments for `five_lemma` and `snake_lemma` sharpened to name the exact
§1.543/§1.55 dependency. No statements changed; no fakes introduced.
