# §1.6 / §1.616 — Pre-logos basics (`Fredy/S1_60.lean`)

## 2026-06-16 — `inverseImage_preserves_unions` STRENGTHENED to `Subobject.le` (landed, build green)

Net-step-1 of the sharpened blocker below has been carried out.  `inverseImage_preserves_unions f`
(and therefore the `PreLogos.invImage_preserves_union` field) no longer concludes a bare object
`Isomorphic (…).dom (…).dom`; it now concludes the mutual `Subobject.le`

```
(InverseImage f (union S T)).le (union (f#S) (f#T))
∧ (union (f#S) (f#T)).le (InverseImage f (union S T))
```

i.e. equality of the two subobjects of `A`.  The two mediating maps now commute with the monics
into `A`, so they CAN serve as the factorizing maps `Subobject.le` requires — exactly what the old
bare iso could not supply.  All three coupled files re-proved green (full `lake build` clean):

- `S1_70.logos_invImage_pres_union` / `logos_implies_preLogos`: the proof already built the two
  directions `hle : join.le fST` and `hle2 : fST.le join`; it used to collapse them via
  `subobject_le_antisymm_iso`.  Now it returns `⟨hle2, hle⟩` directly — no new lemmas, no sorry.
  (`subobject_le_antisymm_iso` is now unused but kept as a generally-useful API lemma.)
- `S1_61.distributive_poset_is_prelogos`: the `hDist` hypothesis still delivers an `Isomorphic`
  (the genuine datum a poset has); in the thin case its two iso-legs `u,v` give both `Subobject.le`s
  with the commuting equation free by `hThin`.  `⟨⟨u, hThin _ _⟩, ⟨v, hThin _ _⟩⟩`.

No instance lacked data: every `PreLogos`-providing site already had both `le` directions (Logos via
the adjunction; poset via thinness).  Grep confirmed no consumer outside these three files reads the
field (S1_64/S1_77 only `extends PreLogos`; S1_72/S1_62/S1_84/S1_94 use only `[PreLogos]` as a
hypothesis or `PreLogos.bottom`).

> **SUPERSEDED (resolved):** the `relSub` bridge described as "still absent" below was subsequently
> built in S1_60 itself (`relSub`, `relSub_union_le/ge`, `relSub_inter_le/ge`, `pushMono`,
> `invImage_mono_local`, `existsAlong_*`).  Consequently **`rel_inter_union_le` is now PROVED**
> (transported across `relSub` from `PreLogos.invImage_preserves_union`), and **`compose_union_right`
> is now PROVED** by the cover-descent route with the single extensive gap isolated in the private
> helper `union_compose_descent`.  The historical analysis below is retained for context only.

### Does this let `rel_inter_union_le` be proved?  NOT YET — and not because of the axiom shape.

The strengthening removes the axiom-shape obstruction (net-step-1).  What remains is purely the
**BinRel ↔ Sub(A×B) bridge**, still absent and still NOT buildable inside the current import frontier:
`rel_inter_union_le` is stated with `relUnion`/`intersect` at the `BinRel` level, while the
strengthened axiom lives on `HasSubobjectUnions.union` over `Sub(prod A B)`.  Closing the gap needs
`relSub : BinRel 𝒞 A B → Subobject 𝒞 (prod A B)` together with
`relUnion R S ≈ HasSubobjectUnions.union (relSub R) (relSub S)` and
`intersect R S ≈ InverseImage (pair R.colA R.colB) (relSub S)`.  The union identity's natural proof
is `union_via_coproduct_image` — which lives in **S1_61** (`import Fredy.S1_60`), so using it from
S1_60 is a circular import; re-deriving it in S1_60 plus adding `[PreLogos 𝒞]` to the `BinRelLattice`
section is a separate, larger refactor.  The `BinRelLattice` section variable line currently carries
no `[HasSubobjectUnions]`/`[PreLogos]`, so the strengthened axiom is not even in scope at
`rel_inter_union_le` as stated.  Conclusion: the axiom is now the RIGHT shape and is consumable;
`rel_inter_union_le` is unblocked *modulo* the still-missing relSub bridge (a follow-on in S1_56/S1_60,
not a re-statement of the axiom).  The two §1.616 `sorry`s (`rel_inter_union_le`, `compose_union_right`)
are left faithful, unchanged.

## Proved (no sorry)

| Theorem | Statement | Notes |
| ------- | --------- | ----- |
| `relUnion_le_left`       | `R ≤ R ∪ᵣ S`                             | via `image.lift m ≫ inl` witness |
| `relUnion_le_right`      | `S ≤ R ∪ᵣ S`                             | via `image.lift m ≫ inr` witness |
| `le_relUnion`            | `R ≤ U → S ≤ U → R ∪ᵣ S ≤ U`           | universal property; uses `image_min` |
| `rel_union_inter_le`     | `(R ∩ S) ∪ (R ∩ T) ≤ R ∩ (S ∪ T)`      | easy direction; no extra axioms |
| `compose_union_right_le` | `(R ⊚ S) ∪ (R ⊚ T) ≤ R ⊚ (S ∪ T)`     | monotonicity of compose |
| `relUnion_le_reciprocal` | `(R ∪ᵣ S)° ≤ S° ∪ᵣ R°`                 | §1.616; cover⊥mono diagonal + swap identity |
| `relUnion_reciprocal_le` | `S° ∪ᵣ R° ≤ (R ∪ᵣ S)°`                 | §1.616; dual by `relUnion_swap_eq S° R°` |
| `rel_inter_union_le`     | `R ∩ (S ∪ T) ≤ (R ∩ S) ∪ (R ∩ T)`      | **PROVED**; transported across `relSub` from `PreLogos.invImage_preserves_union` + `pushMono_union_le` |
| `compose_union_right`    | `R ⊚ (S ∪ T) ≤ (R ⊚ S) ∪ (R ⊚ T)`     | **PROVED** (FULLY, no sorry) by cover-descent; the one extensive gap is isolated in the helper below |

Private helpers:
- `relUnion_swap_eq R S : case(pairR, pairS) ≫ pair snd fst = case(inr,inl) ≫ case(pairS°,pairR°)`.
- `union_compose_descent R S T` — the SHARP extensive gap (see below); the **only** `sorry` in S1_60.

## Sorry (single, faithful, sharply isolated)

| Theorem | Statement | Why sorry |
| ------- | --------- | --------- |
| `union_compose_descent` (private) | `∃ (cover c : P ↠ pb.pt) (φ : P → ((R⊚S)∪(R⊚T)).src), c≫(π₁≫R.colA)=φ≫colA ∧ c≫(π₂≫(S∪T).colB)=φ≫colB` | the extensive split of the coproduct-presenting cover of `(S∪T).src` under pullback along `pb.π₂` (binary coproducts stable under pullback) — genuinely absent in the repo (same blocker flagged at `S1_84.lean:511`) |

### Current state (this agent)

`compose_union_right` is now a **complete, sorry-free reduction**: it builds the image-cover
`eW := image.lift span : pb.pt ↠ (R⊚(S∪T)).src` (`image_lift_cover`), takes the extensive descent
`(P, c, φ)` from `union_compose_descent`, composes the covers (`cover_comp`), and descends `φ` along
`c ≫ eW` via `relLe_of_cover_factor` (cover⊥mono).  `#print axioms compose_union_right` =
`[sorryAx, Classical.choice]` — the `Classical.choice` is the pre-existing `image`/pullback
infrastructure, NOT new; `sorryAx` is the single extensive gap below.

The descent map `φ` is the copairing of the two summand descents **after** `pb1 := pullback(eU, pb.π₂)`
splits over `S.src + T.src` (`eU := image.lift (case (pairS)(pairT))`).  That split = coproduct
extensivity ("pullback distributes over `+`"), strictly stronger than `RegularCategory` /
`PullbacksTransferCovers`, and absent everywhere in the repo (confirmed by full-repo probe; the same
"extensive-category partition lemma … not yet built" is the documented blocker at `S1_84.lean:506-514`).
Given that one primitive, `union_compose_descent` discharges with no new ideas and `compose_union_right`
is then 100% axiom-clean (modulo the ambient `Classical.choice` of `image`).

## Blocker analysis (verified 2026-06-16, two independent sub-agent probes + manual)

Both sorries are the genuine distributivity content of §1.616 (Freyd: "Direct images always
preserve unions").  They are NOT one-liners; each is blocked on infrastructure that does not yet
exist in the repo.  Neither weakening nor faking is acceptable, so the faithful `sorry`s remain.

### `compose_union_right` — the self-contained cover route is blocked on coproduct extensivity

I attempted the same cover⊥mono technique that proves `relUnion_le_reciprocal`.  Full
reconstruction (all steps below typecheck green; only the last fails):

1. `mST = case (pair S.colA S.colB)(pair T.colA T.colB)`; `image.lift mST : coprod S.src T.src →
   (S∪ᵣT).src` is a `Cover` (`image_lift_cover`).
2. `pbST = pullback(R.colB, (S∪ᵣT).colA)`; pull the cover `image.lift mST` back along `pbST.π₂`
   (needs `[PullbacksTransferCovers 𝒞]` via `cover_pullback`) → `Q` with cover leg `q₂ : Q → pbST.pt`
   and `q₁ : Q → coprod S.src T.src`.
3. Pull `q₁` back along `inl`/`inr` → `Q_S, Q_T` with legs `s₁ : Q_S → Q`, `t₁ : Q_T → Q`.
4. Build `dS : Q_S → U.src`, `dT : Q_T → U.src` (U = (R⊚S)∪ᵣ(R⊚T), pU = pair U.colA U.colB monic)
   with `dS ≫ pU = s₁ ≫ q₂ ≫ spanST` and the symmetric `dT`.  Assemble `cmp = case s₁ t₁` and
   `dJoin = case dS dT`; prove `dJoin ≫ pU = cmp ≫ (q₂ ≫ spanST)` by `case_uniq`.  ALL GREEN.
5. **BLOCKED:** to descend `dJoin` to a single `d : Q → U.src` via `cover_mono_diagonal` one needs
   `Cover cmp`, i.e. `Cover (case s₁ t₁)` — exactly that the two pullbacks of `q₁` along the
   coproduct injections JOINTLY COVER `Q`.  That is **coproduct extensivity** (universal + disjoint
   coproducts), strictly stronger than `RegularCategory`/`PullbacksTransferCovers`.

The repo has no extensivity / `Cover (case _ _)` lemma.  `disjoint_cover_is_coproduct`
(S1_61.lean:298) is the CONVERSE (disjoint cover ⟹ coproduct), not coproduct ⟹ joint cover.
`relUnion_le_reciprocal` escapes this because reciprocation is an iso (`pair snd fst`), so the
copairing transports directly with no splitting; composition is not an iso, so it does not.

### Both sorries — the book's route needs a BinRel↔Subobject(A×B) bridge that is missing

Freyd derives §1.616 from `PreLogos.invImage_preserves_union` via two identities:
  `yS = (y×1)#(S)` (precompose by a graph = inverse image along `y×1`) and
  `x∘(yS) = (x×1)(yS)` (postcompose = DIRECT image along `x×1`).
But `relUnion`/`intersect`/`compose` are defined at the **BinRel** level (image of a copairing /
pullback of pairings / image of a span in A×B), while `PreLogos.invImage_preserves_union` is stated
for the **abstract** `HasSubobjectUnions.union` field on `Subobject (prod A B)`.  These two unions
are never identified.  Missing pieces (confirmed absent by grep):
  - no `BinRel 𝒞 A B → Subobject 𝒞 (prod A B)` conversion (the monic `pair R.colA R.colB` is used
    ad hoc but never packaged), nor the inverse;
  - no theorem `relUnion R S ≅ HasSubobjectUnions.union ⟨pairR⟩ ⟨pairS⟩`;
  - no `yS = (y×1)#S` (only `graphMono_prodMap`, S1_92.lean:139, a fragment);
  - no `x∘(yS) = (x×1)(yS)` (composition-as-direct-image) at all.

### What would unblock (precise, for a follow-up)

Add to S1_56/S1_51 (NOT S1_60, which is import-frozen here): (a) `relSub : BinRel 𝒞 A B →
Subobject 𝒞 (prod A B)` and its lattice-iso to `relUnion`/`intersect`; (b) `compose_eq_directImage`:
`R ⊚ S = image((R.colB-pullback)≫(prodMap …))` recast as a direct image of `relSub S` along a
product map; (c) the elementary fact `directImage f (S ∪ T) = directImage f S ∪ directImage f T`
(image preserves joins — provable from `image_min` + `le_relUnion`, no extensivity).  Then
`compose_union_right` is (c) specialized, and `rel_inter_union_le` follows from
`invImage_preserves_union` through (a)+(b).  ALTERNATIVELY add coproduct extensivity and finish the
cover route in step 5 above (the rest is already proven to typecheck).

## SHARPENED blocker (verified 2026-06-16, this agent — third probe, deeper than above)

Three additional facts were established by exhaustively trying the "direct images preserve unions"
route the task recipe favored (the route that is supposed to avoid extensivity).  All three reduce
the two remaining `sorry`s to facts that are **provably absent** at this import frontier, not merely
unwritten.  Faithful `sorry`s therefore remain; **no fake, no vacuous `True`, no `axiom`.**

### (1) The BOOK itself hypothesizes `pre-logos`; the current `compose_union_right` is UNDER-hypothesized.
`categories-allegories.txt` line 6055: "**In a pre-logos** B&(A,B) is a distributive lattice."
The whole §1.616 derivation (`yS=(y×1)#S`, "inverse images preserve unions", `x∘(yS)=(x×1)(yS)`,
"direct images always preserve unions") lives inside a pre-logos.  In a bare regular category
`compose_union_right` is **false** (composition need not distribute over union without the
pre-logos axiom).  So the faithful statement needs `[PreLogos 𝒞]` — but see (3): even WITH it the
axiom as currently stated is too weak.

### (2) Every honest sub-route for `compose_union_right` collapses to the SAME single-map split.
`R ⊚ U = image(spanU)`, `spanU : pbU.pt → A×C`, `pbU = pullback(R.colB, U.colA)`.  I verified that
each of the three standard reductions terminates at "find ONE map `pbST.pt → ((R⊚S)∪(R⊚T)).src`
agreeing with `spanST`":
  - `image_min`/`Allows` directly → needs `Allows V spanST` (one map);
  - `image_cover_comp` (S1_56:1440, `image(c≫f)=image f` for `c` cover) → needs a cover
    `e : coprod pbS.pt pbT.pt ↠ pbST.pt` so that `e≫spanST` factors per-summand;
  - `relLe_of_cover_factor` (S1_56:1490) with cover `image.lift spanST : pbST.pt ↠ (R⊚(S∪T)).src`
    → needs `φ : pbST.pt → V.src` with `φ ≫ pair V.colA V.colB = spanST` (one map again).
The single map exists iff `pbST.pt` splits as `pbS.pt ⊔ pbT.pt` over `coprod S.src T.src`, i.e. iff
pullback along `pbST.π₂` preserves the coproduct `image.lift mST : coprod S.src T.src ↠ (S∪T).src`.
That is **coproduct extensivity** (universal+disjoint coproducts), strictly stronger than
`PullbacksTransferCovers`/`RegularCategory`.  `image.lift mST` being a cover and `cover_pullback`
(regularity) give a cover `Q ↠ pbST.pt`, but `Q = pullback(case…, π₂)` does NOT decompose over the
two injections without extensivity.  So the "direct image preserves joins" content is **trivial at
the raw `image` level** (`case h k ≫ g = case (h≫g)(k≫g)` by `case_uniq`, so
`image(case h k ≫ g)` IS the copairing-union of `image(h≫g)`, `image(k≫g)`) — the ENTIRE
difficulty is `compose_eq_directImage`, and THAT step is where extensivity (or the U-independent
fixed-projection reformulation of `compose`) is unavoidable.  The repo's `compose` uses the
U-DEPENDENT pullback `pullback(R.colB, U.colA)`; the book's route uses the U-INDEPENDENT projection
`π_{BC} : A×B×C → B×C`.  Proving repo-`compose` = projection-`compose` needs a ternary product
`A×B×C`, three projections, `prodMap`, and `directImage`/`InverseImage`-of-relation — **none of
which exist** (grep for `prod (prod`, `prodMap`, `directImage`, `ternary` in S1_45/51/56 → empty).

### (3) Even granting `[PreLogos]`, the axiom `invImage_preserves_union` is TOO WEAK for the goal.
`inverseImage_preserves_unions` (S1_60:69) and `PreLogos.invImage_preserves_union` deliver only
`Isomorphic (…).dom (…).dom` — a **bare object iso** (`Isomorphic A B := ∃ f, IsIso f`, S1_34:24),
with NO commuting condition over `A`.  But `rel_inter_union_le` needs `Subobject.le`, i.e.
`∃ h, h ≫ T.arr = S.arr` (S1_51:31) — the iso must commute with the monic into `A×B`.  `intersect`
(S1_56:151) IS literally `InverseImage eR ⟨eS⟩` (pullback of the two pair-monics), so
`rel_inter_union_le` is exactly inverse-image-preserves-union in `Sub(A×B)` — but the pre-logos
axiom's bare `Isomorphic` cannot supply the commuting `h`.  **Fix:** restate
`inverseImage_preserves_unions f` as a `Subobject.le` (or full `=`) of subobjects of `A`, not an
`Isomorphic` of bare domains.  That is a one-line strengthening in S1_60's own `def`, BUT it then
makes `PreLogos` (S1_52/S1_60 `class`, import-frozen for THIS task) carry a stronger field that
every `PreLogos` instance downstream must re-prove — out of scope here, and a genuine API change.

### Net: minimal unblock set (all in FROZEN files S1_45/51/52/56), in dependency order
  1. S1_60 `def inverseImage_preserves_unions`: `Isomorphic .dom .dom` → `Subobject.le` both ways
     (or `Subobject.equiv`).  Then `rel_inter_union_le` follows from `intersect = InverseImage eR`
     + `relUnion = HasSubobjectUnions.union` (the join-uniqueness iso, provable from
     `relUnion_le_left/right`+`le_relUnion` already in S1_60) + the strengthened axiom.  **No
     extensivity needed for `rel_inter_union_le` — only the axiom-strengthening + the relUnion↔
     abstract-union join-uniqueness bridge.**
  2. For `compose_union_right`: EITHER (a) add coproduct extensivity (`Cover (case s₁ t₁)` from
     joint-cover-of-pullbacks) and finish the typechecked cover route at step 5 above; OR (b) build
     the ternary-product + fixed-projection `compose_eq_directImage` reformulation in S1_56, after
     which join-preservation is the trivial `case_uniq` fact in (2) of the sharpened analysis.
  3. Add `[PreLogos 𝒞]` to BOTH theorem signatures to match the book (line 6055).

## 2026-06-16 — Re-verified (fourth agent pass): both sorries remain faithful, no new discharges

The SHARPENED-blocker section (3) noted that `inverseImage_preserves_unions` was too weak
(`Isomorphic .dom .dom`); that **has already been fixed** (see header above): it now returns
`Subobject.le` both ways.  After the strengthening, the remaining blocker for `rel_inter_union_le`
is purely the BinRel↔Sub(A×B) bridge:

- `InverseImage (pair R.colA R.colB) S_sub` lives in `Sub(R.src)` (subobject of domain of f)
- `relUnion (R⊓S) (R⊓T)` is a `BinRel` (subobject of `A×B`)
- The abstract `HasSubobjectUnions.union` in `Sub(R.src)` is axiomatically given, not concretely
  identified with the coproduct-image construction `relUnion` in `Sub(A×B)`
- Bridging requires `relSub : BinRel → Subobject (prod A B)` + join-uniqueness iso — still absent

The sorry comments in the file were sharpened to document these precise blockers inline.
`pair_fst_snd` (S1_42:107) confirms `pair (S∪T).colA (S∪T).colB = (S∪T)'s image arrow ≫ id
= img.arr`, so `R ⊓ (S∪T)` IS the pullback of `eR` along `img.arr` — but decomposing that
pullback into summands over `S.src ⊔ T.src` still needs coproduct extensivity.

**Build status**: green (`lake build Fredy.S1_60` 54/54 clean).
**Sorry count**: 2 (`rel_inter_union_le`, `compose_union_right`), both faithful §1.616 statements.

## Structure

- `HasSubobjectUnions` — existence of joins in `Sub(B)` for each `B`
- `InverseImage` — pullback of a subobject along a morphism (Subobject of DOMAIN, not codomain)
- `inverseImage_preserves_unions` — predicate: `f*(S₁ ∪ S₂) ≤ f*S₁ ∪ f*S₂` AND reverse (both `Subobject.le`)
- `PreLogos` — extends `RegularCategory` + `HasSubobjectUnions` + `invImage_preserves_union`
- `IsDistributiveLattice` / `poset_prelogos_iff_distributive` — §1.613. INTEGRITY FIX: the old predicate was
  the meet-FREE join-absorption `(A∪S)∪A ≤ A∪(S∪T)`, which holds in EVERY lattice (incl. non-distributive
  N₅/M₃), so it captured nothing. Redefined via MEET as `A ∩ (S∪T) ≤ (A∩S) ∪ (A∩T)` with `A∩X :=
  InverseImage A.arr X`, aligning with S1_61's `monic_inverseImage_iff_distributive` (DRY). The theorem is
  now FULLY PROVEN: the inequality is the forward half of `PreLogos.invImage_preserves_union A.arr`.
- `relUnion` — image of `case(pairR, pairS) : R.src+S.src → A×B`; §1.616 BinRel lattice join
