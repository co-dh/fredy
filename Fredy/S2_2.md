# §2.2 Distributive Allegories — tracker

Freyd & Scedrov, *Categories, Allegories*, §2.2.  File: `Fredy/S2_2.lean` (namespace `Freyd.Alg`).
Composition diagram-order (juxtaposition), reciprocal `°`, order `⊑`, union `∪`, zero `𝟘`.

## §2.21  Distributive allegory (definition + algebraic laws)

| Decl                                   | status | note                                                              |
| -------------------------------------- | ------ | ----------------------------------------------------------------- |
| `DistributiveAllegory` (class)         | done   | zero, union; the equational laws of §2.21 as fields               |
| `le_iff_union_eq_left`                 | done   | lattice duality `R ⊑ S ↔ R ∪ S = S`                               |
| `union_lub`, `le_union_left/right`     | done   | union = least upper bound                                         |

## §2.211  Derived equations

| Decl                  | status | note                                              |
| --------------------- | ------ | ------------------------------------------------- |
| `recip_union`         | done   | `(R ∪ S)° = S° ∪ R°`                              |
| `union_comp_distrib`  | done   | `(S ∪ T)R = SR ∪ TR` (via reciprocation)          |
| `union_zero`          | done   | `R ∪ 𝟘 = R`                                       |
| `zero_le`             | done   | `𝟘 ⊑ R` (zero is the minimum)                     |
| `recip_zero`          | done   | `𝟘° = 𝟘`                                          |

`0_R = 0` (`zero_comp`) and `R 0 = 0` (`comp_zero`) and `O(R∪S) = OR` (source equality, structural)
are taken as the structure's axioms / hold definitionally; no separate theorem needed.

## §2.214  Coproduct (five equations ↔ universal property)

| Decl                                | status        | note                                                      |
| ----------------------------------- | ------------- | --------------------------------------------------------- |
| `Coproduct` (structure)             | done          | the five equations `u₁u₁°=1, u₁u₂°=0, u₂u₁°=0, u₂u₂°=1, u₁°u₁∪u₂°u₂=1` |
| `IsCoproduct` (def)                 | done          | universal property: unique mediator `u₁°R₁ ∪ u₂°R₂`        |
| `coproduct_five_eqs_to_universal`   | done          | five eqs ⟹ universal property (mediator + uniqueness)      |
| `le_recip_of_section`               | done          | helper: `f≫g=1 ∧ g≫f⊑1 ⟹ g ⊑ f°` (modular law `modular_le`) |
| `eq_recip_of_section`               | done          | helper: `f≫g=1 ∧ g≫f⊑1 ⟹ g = f°` (both directions)        |
| `coproduct_of_universal_eqs`        | done          | Prop bundle of the five eqs from the universal property    |
| `coproduct_of_universal`            | done          | universal property ⟹ five eqs.  Full constructive proof of Freyd's §2.214 converse: mediators `p₁,p₂`; eq-5 by uniqueness of the mediator of `(u₁,u₂)`; then `uᵢ = pᵢ°` via `eq_recip_of_section` (modular law).  **No axioms, no choice** (`#print axioms` = ∅). |

## §2.215  Reciprocal duality (coproduct iff product)

Book: "(U₁, U₂) is a coproduct iff (U₁°, U₂°) is a product."

| Decl                       | status | note                                                      |
| -------------------------- | ------ | --------------------------------------------------------- |
| `AlgProduct` (structure)   | done   | reciprocal-dual five product equations `p₁°p₁=1, …, p₁p₁°∪p₂p₂°=1` |
| `AlgProduct.ofCoproduct`   | done   | coproduct ⟹ product (reciprocate the five eqs)            |
| `Coproduct.ofAlgProduct`   | done   | product ⟹ coproduct (the converse)                        |

## §2.215  Positive allegory

| Decl                | status | note                                            |
| ------------------- | ------ | ----------------------------------------------- |
| `PositiveAllegory`  | done   | distributive allegory with finite coproducts    |

## §2.216  Positive reflection A⁺  (matrix allegory)

| Decl                                  | status | note                                           |
| ------------------------------------- | ------ | ---------------------------------------------- |
| `AlgMat`, `.recip/.inter/.union/.zero`| done   | I×J matrices of A-morphisms; entry-wise ops    |
| `positiveReflectionEmbed`             | done   | `R ↦ (R)` 1×1 matrix                            |
| `positiveReflectionEmbed_injective`   | done   | the 1×1 embedding is injective (faithful on it)|
| `positive_reflection_faithful`        | done   | faithfulness of the embedding on its image     |

MISSING (recorded, not faked): **full positivity / fullness of A⁺**, i.e. that `AlgMat` with the
§2.216 composition formula `(RS)_{ik} = ∪_j R_{ij} S_{jk}` is itself a `PositiveAllegory` and the
embedding is *full*.  Blocking reason: there is no `Allegory`/`DistributiveAllegory` *instance* on
`AlgMat` in the repo — composition needs a *finite* indexing union (the book's `∪_j`), which the
bare `DistributiveAllegory` (binary union only) cannot express for arbitrary index `I : Type u`.
Stating "A⁺ is a positive allegory" faithfully requires building that instance (finite-index
matrix composition); not yet in the repo, so SKIPPED rather than stubbed.

## §2.22 / §2.221 / §2.222  Local completion (downdeals / ideals)

| Decl                                      | status | note                                                 |
| ----------------------------------------- | ------ | ---------------------------------------------------- |
| `LocallyCompleteDistributiveAllegory`     | done   | predicate-Sup complete lattice, Sup-distributivity   |
| `IsDowndeal`, `principalDowndeal`         | done   | §2.221 downdeals                                      |
| `principalDowndeal_isDowndeal`            | done   |                                                      |
| `principalDowndeal_injective`             | done   | embedding `R ↦ ↓R` faithful                          |
| `allegory_embeds_in_locally_complete`     | done   | §2.221 faithful representation (on principal downdeals)|
| `IsIdeal`, `principalIdeal`               | done   | §2.222 ideals (downdeal closed under finite union)   |
| `principalIdeal_isIdeal`                  | done   |                                                      |
| `principalIdeal_injective`                | done   | §2.222 faithful representation (on principal ideals) |

MISSING (recorded, not faked): the **allegory structure on downdeals/ideals** (composition =
"downdeal generated by `{RS}`") and that the embedding is a *representation* (preserves ∘, °, ∩, ∪),
plus that `Â` *is* locally complete.  Blocking reason: needs the "downdeal generated by a set"
closure operator and an `Allegory` instance on the downdeal hom-type; not yet built.  The
faithfulness lemmas above are honest (injectivity of `R ↦ ↓R`), but the full representation theorem
is SKIPPED.

## §2.223  Globally complete allegory

| Decl                       | status | note                                                |
| -------------------------- | ------ | --------------------------------------------------- |
| `GloballyCompleteAllegory` | done   | disjoint unions of indexed object families          |

## §2.224  Global completion A′

| Decl                              | status | note                                              |
| --------------------------------- | ------ | ------------------------------------------------- |
| `GlobalObj`, `GlobalMorphism`     | done   | indexed families; infinite matrices               |
| `GlobalMorphism.comp/.recip`      | done   | Sup-composition; entry-wise reciprocation         |
| `globalCompletionEmbed`           | done   | `R ↦ (R)` 1×1 matrix                              |
| `globalCompletionEmbed_injective` | done   | 1×1 embedding injective                            |
| `lc_embeds_in_globally_complete`  | done   | faithfulness of the embedding on its image         |

MISSING (recorded, not faked): A′ *is* a globally complete allegory and the embedding is *full*.
Blocking reason: same as A⁺ — no `Allegory` instance on `GlobalMorphism`; needs the local-Sup
composition to satisfy the allegory laws (associativity via Sup-distributivity).  SKIPPED.

## §2.226  Systemic completion

| Decl                                   | status | note                                                |
| -------------------------------------- | ------ | --------------------------------------------------- |
| `SplitSymmIdem` (structure)            | done   | a symmetric idempotent together with its splitting  |
| `systemic_completion_tabular_effective`| done   | given a splitting, exhibits the split map (honest)   |

MISSING (recorded, not faked): the systemic-completion *construction itself* (split every
symmetric idempotent of A′) and the theorem that it is tabular + effective *as built*.  The current
lemma honestly says: *if* a symmetric idempotent is equipped with a splitting (`SplitSymmIdem`),
then that splitting witnesses tabularity/effectivity — it does not construct the splitting.  The
construction needs A′ first (see §2.224), so it is SKIPPED.

## Integrity note

No `: True` stubs, no vacuous restatements, **no `sorry`**, and no `axiom` beyond the class fields
that are Freyd's actual §2.21 axioms.  `coproduct_of_universal` is now a full constructive proof
(axiom-free).  Genuinely-hard theorems needing unbuilt constructions (full positivity of A⁺, the
downdeal/ideal allegory representation, fullness/global-completeness of A′, the systemic
construction) are recorded as MISSING above and NOT stubbed.
