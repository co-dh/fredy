# §1.92 Tracker — Singleton Map, Topos is Exponential

| Section | Term / Kind                  | Status    | Note                                                                                    |
|---------|------------------------------|-----------|-----------------------------------------------------------------------------------------|
| §1.92   | `topos_has_exponentials`     | sorry     | Topos → HasExponentials; full adjunction needs B^A ≅ [A×B] construction                |
| §1.92   | `singletonMapCat`            | defined   | Δ₁ B = curry(classify(diag B, diag_mono B)); definition compiles                       |
| §1.92   | `singletonMapCat_monic`      | sorry     | Needs classify pullback universality to show monic                                      |
| §1.92   | `powerMapCov`                | sorry     | [f] : [A]→[B] covariant direct-image; needs images + classify interaction               |
| §1.92   | `singletonMapCat_natural`    | sorry     | f ≫ Δ₁(B) = Δ₁(A) ≫ [f]; needs powerMapCov defined first                              |
| §1.921  | `HasPartialMapClassifier`    | defined   | Structure with pmc_obj, pmc_incl, pmc_classify; no sorries                              |
| §1.921  | `LawvereTopos`               | defined   | Class extends HasExponentials + has_coproducts + has_coterminator + has_pmc; no sorries |
| §1.922  | `omegaPowContra`             | partial   | ContraFunctor instance; map_id proven; map_comp sorry (needs double curry computation)  |
| §1.923  | `expSubobj`                  | sorry     | ∃ monic ι : exp A B → exp(A×B) Ω; construction via pullback of Ω^{fst} deferred       |
| §1.924  | (comment)                    | note      | FG(A) = (G(-), F(A+-)); presheaf Yoneda computation; no formalization possible yet     |
| §1.926  | `SubTerminal`                | defined   | Type alias for one ⟶ omega                                                              |
| §1.926  | `heytingImpl`                | sorry     | U ⇒ V computed via Ω^U : Ω^Ω → Ω^1 ≅ Ω; needs canonical iso Ω^1 ≅ Ω                 |
| §1.926  | `subTerminal_heyting`        | proven    | ∃ W = heytingImpl U V; just rfl once heytingImpl defined                                |

## Blockers

1. **`topos_has_exponentials`** — needs `exp_obj A B = exp (prod A B) omega` plus the currying adjunction lifted from power objects. Core of Kock's observation (§1.921).
2. **`omegaPowContra.map_comp`** — the proof requires unfolding `prodMap` of a composition of curried maps and applying `curry_eval_eq` twice. Elaboration of implicit product-object arguments is tricky.
3. **`singletonMapCat_monic`** — needs the classify pullback universality property (only the existence of classify is in `HasSubobjectClassifier`, not the pullback square axiom).
4. **`powerMapCov`** — needs image factorization applied to the graph of f; requires `HasImages` to be derived from `Topos`.
5. **`expSubobj`** — depends on `powerMapCov` and `singletonMapCat_monic`.
6. **`heytingImpl`** — needs canonical iso `exp one omega ≅ omega` (i.e. Ω^1 ≅ Ω).

## Build status

`lake build Fredy.S1_92` succeeds with zero errors; 7 sorry warnings (all faithful statements).
