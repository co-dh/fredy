# §1.92 Tracker — Singleton Map, Topos is Exponential

| Section | Term / Kind                    | Status   | Note                                                                                |
|---------|--------------------------------|----------|-------------------------------------------------------------------------------------|
| §1.92   | `topos_has_exponentials`       | sorry    | Topos → HasExponentials; book route via §1.859 baseable-equalizers (itself sorry)   |
| §1.92   | `curry_precomp`                | PROVEN   | `h ≫ curry f = curry (prodMap A _ _ h ≫ f)` — adjoint-transpose naturality of A×−  |
| §1.92   | `singletonMapCat`              | defined  | Δ₁ B = curry(classify(diag B, diag_mono B))                                        |
| §1.92   | `graphMono`/`graph_classifies` | PROVEN   | graph monic ⟨h,1⟩↪B×X' and char map = B×h ≫ χ_Δ (pullback by pasting diag square) |
| §1.92   | `singletonMapCat_monic`        | PROVEN   | Δ₁ monic; via curry_precomp + curry_inj + graph_classifies pullback uniqueness     |
| §1.92   | `powerMapCov`                  | sorry    | [f] : [A]→[B] direct image; needs §1.56 image factorization packaged on power objs  |
| §1.92   | `singletonMapCat_natural`      | sorry    | f≫Δ₁(B) = Δ₁(A)≫[f]; blocked by powerMapCov (equation IS [f]'s defining property)  |
| §1.921  | `HasPartialMapClassifier`      | defined  | Structure; no sorries                                                               |
| §1.921  | `LawvereTopos`                 | defined  | Class; no sorries                                                                   |
| §1.922  | `omegaPowContra.map`           | defined  | Ω^g = curry(pair(fst≫g) snd ≫ eval); contravariant action                          |
| §1.922  | `omegaPowContra.map_id`        | PROVEN   | Ω^(id) = id                                                                         |
| §1.922  | `omegaPowContra.map_comp`      | PROVEN   | Ω^(f≫g) = Ω^g ≫ Ω^f; via curry_unique_eq + coordinatewise pair pushforwards        |
| §1.923  | `expSubobj`                    | sorry    | ∃ monic ι : B^A → [A×B]; `exp A B` opaque while `topos_has_exponentials` is sorry  |
| §1.924  | (comment)                      | note     | FG(A)=(G(−),F(A+−)); presheaf Yoneda — no formalization yet                        |
| §1.926  | `SubTerminal`                  | defined  | Type alias one ⟶ omega                                                              |
| §1.926  | `heytingImpl`                  | PROVEN   | U⇒V = curry(snd≫V) ≫ Ω^U ≫ prodOneLeftInv ≫ eval; uses S1_42 left-unitor          |
| §1.926  | `subTerminal_heyting`          | PROVEN   | ∃ W = heytingImpl U V, proved by rfl                                                |

## Discharged this pass

- **`curry_precomp`** — reusable naturality lemma used by the monic proof.
- **`omegaPowContra.map_comp`** — full contravariant-functoriality of Ω^(−). Reduces RHS `Ω^g ≫ Ω^f`
  through `curry_unique_eq`, expands the inner `curry` via `curry_eval_eq`, and pushes the reindexing
  `pair (fst≫·) snd` through `prodMap` coordinatewise using `pair_uniq`.
- **`singletonMapCat_monic`** — Δ₁ is monic (the book's "as a consequence, Δ₁ is monic"). Built three
  helpers: the graph monic `graphMono h = ⟨h,1⟩ : X'↪B×X'` (monic via retraction `snd`), the identity
  `graphMono h ≫ (B×h) = h ≫ diag B`, and the key lemma `graph_classifies`: `B×h ≫ χ_Δ` is the
  characteristic map of `graphMono h`, with the graph square a pullback (obtained by pasting the diagonal
  classifier pullback with the pullback of the diagonal along `B×h`, via `classify_pullback`). Then if
  `h ≫ Δ₁ = k ≫ Δ₁`, `curry_precomp` + `curry_inj` give equal char maps, so `graphMono k` is a cone over
  `graphMono h`'s pullback; the lift `u` satisfies `u ≫ graphMono h = graphMono k`, hence (project `snd`)
  `u = 1` and (project `fst`) `h = k`.
- **`heytingImpl`** (this pass, new discharge) — U⇒V implemented as:
  `curry(snd ≫ V) ≫ omegaPowContra.map U ≫ prodOneLeftInv(Ω^^one) ≫ eval_exp one Ω`.
  - `curry(snd ≫ V) : one → Ω^^Ω` names V as the "constant" Ω-valued function via snd : prod Ω one → one.
  - `omegaPowContra.map U : Ω^^Ω → Ω^^one` is the contravariant Ω^U action (already proved in §1.922).
  - `prodOneLeftInv(Ω^^one) ≫ eval_exp one Ω : Ω^^one → Ω` is the left-unit iso Ω^1 ≅ Ω,
    available from S1_42 (newly imported). The blocker comment was wrong: `prod_one_iso_left` and
    `prodOneLeftInv` ARE in S1_42; adding `import Fredy.S1_42` unblocked this.

## Remaining blockers (all faithful sorries on real book statements)

1. **`topos_has_exponentials`** — book proof (§1.92): "power objects are baseable; B is an equalizer of
   χ and [B]→1→Ω; so B is baseable [§1.859]". Crux = `baseable_inclusion_preserves_equalizers` (sorry in
   S1_85) plus the power-object⇒baseable adjunction packaging. Requires §1.859 to be finished first.
2. **`powerMapCov`** — the existential direct image [f] : [A] → [B]. Needs §1.56 regular image
   factorization assembled into a morphism `exp A Ω → exp B Ω` (the ∃_f action on the universal relation).
   §1.56's `image` exists but is not packaged as a power-object morphism.
3. **`singletonMapCat_natural`** — f(Δ₁)=Δf. Stated against `powerMapCov f`, which is unfilled; the
   equation is precisely [f]'s defining property, so it is not independently provable until [f] is defined.
4. **`expSubobj`** — `exp A B` is opaque because `topos_has_exponentials` is sorry; no concrete monic
   `ι : B^A → [A×B]` can be exhibited until exponentials are constructed (the embedding would be
   `curry(eval_A_B ≫ singletonMapCat B)` composed with the curry iso `B^1^A ≅ (A×B)^1`, once available).

## Build status

`lake build Fredy.S1_92` succeeds with **zero errors**.  
Sorry count: **5 sorry keywords** in 4 declarations (down from 6 keywords / 5 declarations before this
pass), all on faithful book statements with documented blockers. `heytingImpl` was discharged by adding
`import Fredy.S1_42` and constructing the implication via the Ω^U action and the left-unit iso Ω^1 ≅ Ω.
