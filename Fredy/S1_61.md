# §1.61 Pre-logoi — Tracker

| Section | Term / Statement                                         | Kind      | Status  | Note                                                                                   |
|---------|----------------------------------------------------------|-----------|---------|----------------------------------------------------------------------------------------|
| §1.61   | 0 = minimal subobject of 1 is a coterminator            | def       | DONE    | `minimal_subobject_of_one_is_coterminator`                                             |
| §1.61   | Any morphism to 0 is an isomorphism                     | theorem   | DONE    | `any_map_to_zero_is_iso`                                                               |
| §1.61   | Pre-logos is degenerate iff 0 ≅ 1                       | theorem   | DONE    | `degenerate_iff_zero_iso_one`                                                          |
| §1.611  | Cartesian + images + covers-transfer ⇒ pre-logos        | theorem   | PARTIAL | `cartesian_with_images_covers_implies_prelogos`. Regular + lattice fields DONE. **2 sorries remain**: (1) `invImage_preserves_union` forward `f#(S∪T) ≤ f#S∪f#T`: blocker is §1.615 (union = image of `case`, requires binary coproducts absent from bare `RegularCategory`) or §1.45 pullback-pasting (still sorry). The **reverse** `f#S∪f#T ≤ f#(S∪T)` IS discharged via `inverseImage_mono` + `union_min`. (2) `invImage_preserves_bottom`: needs `InverseImage f (⊥_B) ≅ ⊥_A`. Only `⊥_A ≤ InverseImage f (⊥_B)` follows from `bottom_min`; the reverse `InverseImage f (⊥_B) ≤ ⊥_A` requires a factorization `φ ≫ ⊥_A.arr = π₁` not constructible from `hBottom_dom_iso` alone (iso of domains doesn't track the monic arrows) nor from `PullbacksTransferCovers` (which only transfers covers, not monic inclusions). Both are faithful sorries — genuinely requires covers-transfer on `0 → 1` or the full §1.61 argument. |
| §1.612  | f# (monic) preserves unions ↔ Sub(B) distributive lattice | theorem | DONE    | `monic_inverseImage_iff_distributive`; ← direction discharged via subobject identity  |
| §1.613  | Thin-category semi-lattice is pre-logos ↔ distributive  | def       | DONE    | `distributive_poset_is_prelogos`; sorry-free. `invImage_preserves_union` returns the strengthened `∧`-of-two-`Subobject.le` via `refine And.intro` + `Exists.intro u/v (hThin _ _)`. `invImage_preserves_bottom` via `thin_iso_of_maps` (thin case: maps in both directions trivially commute). |
| §1.614  | Representation of pre-logoi (functor preserving structure) | class   | DONE    | `PreLogosFunctor` class                                                                |
| §1.615  | Union = image of coproduct map [x₁,x₂]                 | theorem   | DONE    | `union_via_coproduct_image`                                                            |
| §1.616  | Relations form a distributive lattice; R(S∪T)=(RS)∪(RT) | comment  | PARTIAL | Lattice part follows from `monic_inverseImage_iff_distributive`; relational-composition half deferred to S2_* (allegory files) |
| §1.621  | Disjoint cover ⇒ coproduct                              | theorem   | PARTIAL | `disjoint_cover_is_coproduct`; faithful sorry. BLOCKER: both existence and uniqueness are out of reach at the `[PreLogos]` layer. **Uniqueness** fails because `A₁.arr, A₂.arr` being jointly epic requires the union is the image of `case A₁.arr A₂.arr` (§1.615, needs binary COPRODUCTS) — `union_left/right/min` give no joint-epi. **Existence** requires Freyd's relational `R = x°f ∪ y°g` (Chapter-2 allegory `S2_*`). Coproducts exist only in POSITIVE pre-logoi (§1.623). |

## Sorry inventory (final)

3 sorry terms remain across 2 declarations:

1. `cartesian_with_images_covers_implies_prelogos` line 188: forward `f#(S∪T) ≤ f#S∪f#T` — BLOCKER: needs coproducts (§1.615) or pullback-pasting (§1.45, still sorry).
2. `cartesian_with_images_covers_implies_prelogos` line 193: `invImage_preserves_bottom` — BLOCKER: `InverseImage f ⊥_B ≅ ⊥_A` not derivable from abstract `hBottom_dom_iso` + `hBottom_min` alone; needs covers-transfer on the zero monic.
3. `disjoint_cover_is_coproduct` line 338 — BLOCKER: §1.621 needs §1.62 pasting lemma (Chapter-2 relational algebra) or binary coproducts (§1.623).

All statements are faithful to the book. Integrity check clean: no `: True`, no `axiom`, no `trivial` fillers. Build: `lake build Fredy.S1_61` green.
