# §1.61 Pre-logoi — Tracker

| Section | Term / Statement                                         | Kind      | Status  | Note                                                                                   |
|---------|----------------------------------------------------------|-----------|---------|----------------------------------------------------------------------------------------|
| §1.61   | 0 = minimal subobject of 1 is a coterminator            | def       | DONE    | `minimal_subobject_of_one_is_coterminator`                                             |
| §1.61   | Any morphism to 0 is an isomorphism                     | theorem   | DONE    | `any_map_to_zero_is_iso`                                                               |
| §1.61   | Pre-logos is degenerate iff 0 ≅ 1                       | theorem   | DONE    | `degenerate_iff_zero_iso_one`                                                          |
| §1.611  | Cartesian + images + covers-transfer ⇒ pre-logos        | theorem   | PARTIAL | `cartesian_with_images_covers_implies_prelogos` (faithful, from `RegularCategory` = Cartesian+images+covers-transfer + bottom data). Regular + lattice fields DONE. **`invImage_preserves_union`**: strengthened axiom = `f#(S∪T) ≤ f#S∪f#T  ∧  f#S∪f#T ≤ f#(S∪T)`. The **reverse** conjunct is now DISCHARGED via the new sorry-free helper `inverseImage_mono` (= `invImg_le` at the canonical `HasPullbacks`) + `union_min`. Only the **forward** `f#(S∪T) ≤ f#S∪f#T` stays `sorry`: with only abstract lattice axioms the supplied `union` carries no link to images/covers, so f#(join) is uncomputable — needs §1.615 (`union = image of case`, requires binary COPRODUCTS absent from `RegularCategory`) or §1.45 pullback-pasting (itself `sorry`). **`invImage_preserves_bottom`**: still `sorry` — needs an *iso*; minimality gives only `⊥_A ≤ f#(⊥_B)`, the reverse forces f#(⊥_B) to BE ⊥, derivable only from covers-transfer on `0→B`. |
| §1.612  | f# (monic) preserves unions ↔ Sub(B) distributive lattice | theorem | DONE    | `monic_inverseImage_iff_distributive`; ← direction discharged via subobject identity  |
| §1.613  | Thin-category semi-lattice is pre-logos ↔ distributive  | def       | DONE    | `distributive_poset_is_prelogos`; sorry-free. `invImage_preserves_union` now returns the strengthened `∧`-of-two-`Subobject.le`: fixed by `refine And.intro` (split the And, NOT anonymous `⟨,⟩` — the 1st conjunct `.le` is a `def`-to-`∃`, so a nested anon-constructor mis-parses the outer And as `Exists.intro`) then `Exists.intro u/v (hThin _ _)` per leg, using the two iso legs of `hDist ⟨_A,_f,thin-mono⟩`. `invImage_preserves_bottom` via `thin_iso_of_maps` |
| §1.614  | Representation of pre-logoi (functor preserving structure) | class   | DONE    | `PreLogosFunctor` class                                                                |
| §1.615  | Union = image of coproduct map [x₁,x₂]                 | theorem   | DONE    | `union_via_coproduct_image`                                                            |
| §1.616  | Relations form a distributive lattice; R(S∪T)=(RS)∪(RT) | comment  | PARTIAL | Lattice part follows from `monic_inverseImage_iff_distributive`; relational-composition half deferred to S2_* (allegory files) |
| §1.621  | Disjoint cover ⇒ coproduct                              | theorem   | PARTIAL | `disjoint_cover_is_coproduct`; faithful sorry. BLOCKER (re-checked under strengthened axiom + with §1.615 now proved): both the *existence* and the *uniqueness* clause are out of reach at the `[PreLogos 𝒞]` layer. Even uniqueness fails: from `Aᵢ.arr ≫ h' = Aᵢ.arr ≫ h` one needs `A₁.arr, A₂.arr` *jointly epic* to cancel; the union's inclusions `l₁,l₂` are jointly epic only because the join is the IMAGE of `case A₁.arr A₂.arr` (§1.615), which needs binary COPRODUCTS that a bare `PreLogos` lacks (`union_left/right/min` give no joint-epi). §1.615 `union_via_coproduct_image` IS proved but its `[HasBinaryCoproducts 𝒞]` hypothesis is unavailable here. Existence additionally needs Freyd's relational `R = x°f ∪ y°g` (Chapter-2 allegory `S2_*`). Coproducts exist only in POSITIVE pre-logoi (§1.623). |
