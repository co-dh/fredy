# §2.435–§2.436 — The diagonal proofs (Cantor), algebraic form

Freyd & Scedrov, *Categories and Allegories*, Chapter 2, §2.4 (Power allegories), the "diagonal proofs"
subsection.  Formalized in `Fredy/S2_43.lean`.

## What the book says

- **§2.435.** Cantor: for any set `A`, its power-object `[A]` cannot appear as a sub-quotient of `A`; the same
  holds in any non-degenerate topos.  Freyd converts this to an algebraic statement: if a power allegory has a
  morphism `F : A → [A]` with `F°F = 1` (a partial map covering `[A]`), then `T = F∋` is thick, so `A` would
  carry a thick endomorphism.  But: *if a connected division allegory has a thick endomorphism then it is
  equivalent to the one-object one-morphism allegory.*  This reduces to:

- **§2.436.** *The equational theory of one-object pre-power allegories is inconsistent.*
  Define the "diagonal" relation `R = 𝟘 / (1 ∩ T)`, characterised by `S ⊑ R  ↔  S(1∩T) = 𝟘`.  By §2.431 there is
  a witness `R̄` (`= R/T`) with

  - (i)  `1 ⊑ R̄ R̄°`         (entire),
  - (ii) `R̄ T ⊑ R̄`,
  - (iii)`R̄° R̄ ⊑ T`,         and "hence `R̄ ⊑ R`".

  Freyd's diagonal chain is then

  ```
  R̄  ⊑  R̄ (1 ∩ R̄° R̄)  ⊑  R̄ (1 ∩ T)  ⊑  R (1 ∩ T)  ⊑  𝟘 ,
  ```

  so `R̄ = 𝟘`, and entireness (i) forces `1 ⊑ R̄ R̄° = 𝟘`, i.e. `1 = 𝟘`.

## What is formalized

All on the existing allegory infrastructure (no new axioms, no `Mathlib.CategoryTheory`).  DRY reuse:

- `DivisionAllegory`, right division `R/S`, `le_div_iff`, `div_comp_eq_le` — from `S2_3`.
- `𝟘`, `zero_le`, `zero_comp` — from `S2_2`.
- `modular_le`, `inter_lb_*`, `le_inter`, `recip_*`, `comp_mono_*`, `le_antisymm`, `le_trans` — from `S2_1`.
- `Thick`, `PrePowerAllegory` (= §2.43) — from `S2_4`; **not redefined here**.

New declarations in `S2_43.lean`:

| name                                | status        | content                                                            |
|-------------------------------------|---------------|--------------------------------------------------------------------|
| `diag T`                            | def           | the diagonal relation `𝟘 / (1 ∩ T)` (§2.436)                       |
| `le_diag_iff`                       | **proved**    | `S ⊑ diag T ↔ S(1∩T) = 𝟘` (§2.436 characterisation)               |
| `diag_comp_le_zero`                 | **proved**    | `(diag T)(1∩T) ⊑ 𝟘`                                                |
| `le_comp_codom`                     | **proved**    | codomain factorization `R ⊑ R (1 ∩ R°R)` (reciprocal of §2.122)    |
| `inconsistency_core`                | **proved**    | the §2.436 diagonal argument: the witness collapses to `𝟘`, `1 = 𝟘`|
| `one_object_pre_power_inconsistent` | faithful sorry| §2.436 main, from `Thick T`; 1 sorry on `codBox (diag T) = codBox T` |

`inconsistency_core` (the mathematical heart) is **complete** and `lean_verify` reports it depends only on
`propext` — no `sorryAx`, no choice.

## The single `sorry` (honest)

`one_object_pre_power_inconsistent` invokes `thick_iff_existential` on `R = diag T`, obtaining a witness
`W = diag T /ₛ T` with `Entire W`, `W ≫ T ⊑ diag T`, `W° ≫ diag T ⊑ T`.

The rest of the proof is **complete**:
- From `W ≫ T ⊑ diag T` and coreflexive idempotency of `1 ∩ T`:
  `W ≫ (1∩T) ⊑ diag T` and `(W ≫ (1∩T)) ≫ (1∩T) = W ≫ (1∩T) ⊑ diag T ≫ (1∩T) ⊑ 𝟘`,
  so `W ≫ (1∩T) = 𝟘`, giving `W ⊑ diag T` by `le_diag_iff`.
- Then `W° ≫ W ⊑ W° ≫ diag T ⊑ T`, and `inconsistency_core` closes the goal.

The **only** remaining gap is the codomain-box guard `codBox (diag T) = codBox T` needed to call
`thick_iff_existential`, i.e. `Cat.id a ∩ (diag T)° ≫ diag T = Cat.id a ∩ T° ≫ T`.
This reduces to `(diag T)° ≫ diag T = T° ≫ T` — an allegory equation about the "coend" of
`𝟘/(1∩T)` — which is not derivable from the allegory axioms alone; it requires §2.431 infrastructure about
how right division `R/S` behaves with respect to codomain coreflexives (not yet in S2_3/S2_4).

## Census note (Chapter 2)

The level-2 sections §2.1–§2.5 already have files `S2_1`…`S2_5`.  §2.43 (`PrePowerAllegory`, `Thick`, §2.431,
§2.432) lives in `S2_4`, but the §2.435–§2.436 *diagonal proofs* were not yet formalized; this file fills that
gap.  §2.435's "connected ⟹ trivial" packaging needs a notion of *connected allegory* + *equivalence of
allegories* not yet in the repo, so only its reduction target §2.436 (the equational inconsistency) is
formalized.
