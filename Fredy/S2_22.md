# §2.228 — Union allegories (`Fredy/S2_22.lean`)

Freyd & Scedrov, *Categories, Allegories*, §2.228 (the final remark of §2.2, "Distributive
allegories").

## What the book says

Freyd considers **allegories with finite unions which distribute with composition, but not
necessarily with intersection**. A *distributive* allegory (§2.21) is exactly such a structure
that *also* satisfies the intersection-over-union law

```
R ∩ (S ∪ T) = (R ∩ S) ∪ (R ∩ T).
```

He then records three observations:

| # | statement | proof idea (book) |
|---|-----------|-------------------|
| (a) | A **tabular** allegory with this property is distributive. | Tabulate `R ∪ S ∪ T` by `(f,g)`; the poset `{Q ⊑ f°g}` is isomorphic to the coreflexives of `□f`, which form a distributive lattice (∩ = composition on coreflexives). |
| (b) | A **semi-simple** allegory with this property is distributive. | Split the symmetric idempotents to get a tabular allegory with the same property, then apply (a). |
| (c) | An allegory with this property in which **every morphism is the union of the semi-simple morphisms it contains** [2.225] **need not** be distributive. | Witness: for a group `G` with `|G| > 2`, adjoin a zero `0` and a maximal element `M` to the one-object allegory on `G` (`G` discretely ordered, `RM = MR = M` for `R ≠ 0`, `0` absorbing, reciprocation = inverse fixing `0`,`M`). |

The point of (c) is that the union-of-semi-simples hypothesis alone is *weaker* than tabularity
or semi-simplicity: it does **not** force distributivity.

## What is formalized

- `UnionAllegory 𝒜` — the §2.228 structure: the `DistributiveAllegory` data/laws of §2.21
  **minus** the single axiom `inter_union_distrib`. It keeps zero, union (commutative,
  idempotent, associative, zero-unit, absorption with ∩), zero absorbing under composition, and
  **composition distributing over union**. The omitted ∩-over-∪ law is documented in a comment
  inside the class.
- `instance distributiveAllegory_isUnionAllegory` — every `DistributiveAllegory` (§2.21) is a
  `UnionAllegory`. (Proved; establishes the structure is genuinely weaker, not vacuous.)
- `InterUnionDistrib 𝒜` — the predicate "the omitted law holds"; a union allegory is
  distributive iff this holds.
- `UnionOfSemiSimpleUA R` — "`R` is the union of the semi-simple morphisms it contains" (§2.225),
  as a finite list of semi-simple sub-morphisms whose union is `R`.

### Order theory for union allegories (fully proved, reused below)

The lattice axioms shared with §2.21 (the absorption laws + union semigroup laws) already
recover the order theory of `∪` — **none** of these proofs needs the absent `inter_union_distrib`:

- `le_iff_unionU_eq_left`, `unionU_lub`, `le_unionU_left/right`, `unionU_mono`.
- `interUnionU_distrib_ge` — the always-free **easy half** `(R∩S) ∪ (R∩T) ⊑ R ∩ (S∪T)`.
- `interUnionDistrib_iff_le` — `InterUnionDistrib` reduces to the **reverse** containment
  `R ∩ (S∪T) ⊑ (R∩S) ∪ (R∩T)`; the easy half is for free.

### Tabulation deflation calculus (fully proved)

Constructive half-identities for a tabulation `(f,g)` with `f°f = g°g = 1` and `Q ⊑ f g°`:

- `tab_phi_coreflexive` — `φ Q = f° Q g ⊑ 1` (the `φ`-image is coreflexive on the apex).
- `tab_deflate_source` — `f° Q ⊑ g°`.
- `tab_deflate_target` — `Q g ⊑ f`.

These are the genuine, choice-free tabulation calculus available without idempotent splitting.

### §2.213 / §2.226 idempotent-splitting transport calculus (fully proved, sorry-free)

The constructive core of §2.228(a),(b): the systemic/Cauchy-completion transport that carries the
lattice of `{Q ⊑ U}` onto the coreflexives of the apex, where ∩ = composition and the lattice is
distributive. All sorry-free (`propext` only):

- `recip_unionU` — `(R ∪ S)° = R° ∪ S°` in a union allegory (from the lub characterisation of `∪`).
- `unionU_comp_distrib` — right distribution `(S ∪ T) g = Sg ∪ Tg` (reciprocal of the left axiom).
- `phi_unionU` — the transport `φ Q = f° Q g` preserves union: `φ(S∪T) = φS ∪ φT`.
- `phi_psi_apex` — `φ(ψ A) = f° (f A g°) g = A` for apex coreflexives `A` (the round-trip that
  *does* hold unconditionally; `ψ∘φ = id` is the one that fails — see below).
- `coreflexive_inter_unionU_distrib` — **the coreflexive distributive law** `A ∩ (B∪C) =
  (A∩B) ∪ (A∩C)` for coreflexives, via `coreflexive_comp_eq_inter` (§2.121) + `comp_union_distrib`.
- `interUnionU_distrib_of_transport` — **§2.213/§2.226 transport theorem**: from a coreflexive-valued
  retraction `ψ` of `φ` on `{Q ⊑ U}` with `ψ∘φ = id` (`hsplit*`) preserving ∩ and ∪
  (`hψcap`, `hψcup`), it derives the full ∩-over-∪ *equality* `R ∩ (S∪T) = (R∩S) ∪ (R∩T)`, hence
  the §2.228(a) containment. These bridge hypotheses are *exactly* the splitting of the symmetric
  idempotent `□f` — theorems in an effective allegory, the sole remaining gap for a bare tabular one.

### Theorems

| name | book | status |
|------|------|--------|
| `interUnionDistrib_of_tabular` | §2.228(a) | **proved** modulo `tab_transport_gap`: reduces (via `interUnionDistrib_iff_le`) to the reverse containment; tabulates `R∪S∪T`, derives `f°f = g°g = 1`, shows the `φ`-images `f°·g` of `R,S,T` are coreflexive (`tab_phi_coreflexive`), then calls the isolated gap |
| `tab_transport_gap` | §2.228(a) core | faithful `sorry` on a **true** statement (holds in `Rel`). Reduced via `interUnionU_distrib_of_transport`: the only residual is producing the `ψ`/`hsplit*`/`hψcap`/`hψcup` splitting of `□f`. That splitting is **not** elementary — the round-trip `ψ(φ Q) = Q` (equivalently order-reflection of `φ`) is **FALSE** for a bare tabulation (Lean-verified `Rel` singleton below); only §2.213/§2.226 effective-allegory splitting repairs it, and that is absent from the repo |
| `interUnionDistrib_of_semiSimple` | §2.228(b) | **proved** modulo `semiSimple_transport_gap`: reduces to the reverse containment and feeds the semi-simple witnesses |
| `semiSimple_transport_gap` | §2.228(b) core | faithful `sorry` — split symmetric idempotents to land in a tabular union allegory and apply (a); blocked by the same missing splitting infra (and transitively by (a)) |
| `exists_unionOfSemiSimple_not_distributive` | §2.228(c) | **PROVED, sorry-free** — explicit `ℤ/3 ∪ {𝟎, M}` model |

### Why §2.228(a)'s transport is not elementary (correction to the book's one-liner)

Freyd states `{Q ⊑ f°g} ≅ Coref(apex)` via `Q ↦ 1 ∩ f° Q g`, `A ↦ f A g°`. The forward direction
`φ(ψ A) = A` holds (`phi_psi_apex`), but the **other round-trip `ψ(φ Q) = Q` (equivalently the
order-reflection `φ Q₁ ⊑ φ Q₂ → Q₁ ⊑ Q₂`) FAILS** for a bare tabulation. Lean-verified witness in
`Rel` (`lean_run_code`): apex `c = {∗}`, `a = p = {1,2}`, `f, g` the unique total maps `{1,2} → {∗}`.
Both are maps and `f°f = g°g = 1_c` — a valid `Tabulates` apex — with `U = f g° = ⊤`. But `φ`
collapses every nonempty `Q ⊑ U` to `1_c`: `φ{(1,1)} = φ{(1,2)} = 1_c` while `{(1,1)} ⋢ {(1,2)}`,
and `ψ(φ Q) = f f° Q g g° = ⊤ ≠ Q`. The defect: `(f,g)` is *not a monic pair*
(`f f° ∩ g g° = ⊤ ≠ 1`; the meet is not even well-typed when `a ≠ p`), so the apex is a proper
quotient of the true subobject lattice. Only **splitting** `□f` (§2.213, systemic completion §2.226,
available in an *effective* allegory — not a merely *tabular* one) repairs the apex so that `ψ`
becomes a genuine ∩/∪-preserving section. With that section, `interUnionU_distrib_of_transport`
closes §2.228(a) outright. That effective/systemic-completion machinery is not in this repo — this
is the faithful sorry.

### The §2.228(c) counterexample (fully constructive)

Built explicitly as the one-object allegory `Q3 = {𝟎, e, a, a², M}` (`G = ℤ/3`, |G| = 3 > 2):

- `Q3.meet`/`Q3.join` (discrete lattice on `G`, `𝟎` bottom, `M` top), `Q3.comp` (group product,
  `𝟎` absorbing, `M` maximal), `Q3.recip` (inverse, `a° = a²`).
- `Cat Unit`, `counterAllegory : Allegory Unit`, `counterUnionAllegory : UnionAllegory Unit` —
  **every** allegory and union-allegory axiom discharged by finite case analysis (`cases … <;> rfl`).
- `Q3.simple_of` — the simples are exactly `{𝟎, e, a, a²}` (not `M`); `Q3.semiSimple_of`,
  `counter_unionOfSemiSimple` — every morphism is a union of semi-simples it contains
  (non-`M` ones are themselves semi-simple; `M = e ∪ a`).
- Non-distributivity at `R = a, S = e, T = a²`: `a ∩ (e ∪ a²) = a ∩ M = a`, but
  `(a∩e) ∪ (a∩a²) = 𝟎 ∪ 𝟎 = 𝟎`.

`#print axioms` confirms `exists_unionOfSemiSimple_not_distributive` (and all the (c) lemmas,
the iff-reduction, `tab_phi_coreflexive`, `tab_deflate_source/target`) depend on **`propext`
only** — no `Classical.choice`, no `sorryAx`.

Each remaining `sorry` sits inside the book's real mathematical statement. None is `: True`, a
vacuous `trivial`, an unused-hypothesis stub, or an over-promising name. The hypotheses in (a),(b)
(`Tabular`/`SemiSimple` everywhere) are genuinely consumed: (a) extracts the tabulation and the
`f°f=g°g=1` identities and the coreflexive `φ`-images; (b) feeds the semi-simple witnesses to its
gap. The two gaps are sharply-stated, single-purpose lemmas.

## DRY / reuse

Everything is reused from `S2_1`/`S2_2`:

- `Allegory`, the order `⊑`, `∩`, `°` — `S2_1`.
- `Tabular`, `SemiSimple`, `Simple` — `S2_1`.
- `DistributiveAllegory` (for the comparison `instance` and to mirror the axiom list) — `S2_2`.

The new class mirrors `DistributiveAllegory` exactly but drops one axiom, so the two structures
are directly comparable; no allegory definition is duplicated.

## Sorry count

**2**, both honest and sharply isolated: `tab_transport_gap` and `semiSimple_transport_gap`, each
now reduced to the single missing piece — splitting the symmetric idempotent `□f` (§2.213/§2.226)
to supply the `ψ`/`hsplit*`/`hψcap`/`hψcup` bridge of the sorry-free
`interUnionU_distrib_of_transport`. The §2.213/§2.226 transport calculus itself
(`recip_unionU`, `unionU_comp_distrib`, `phi_unionU`, `phi_psi_apex`,
`coreflexive_inter_unionU_distrib`, `interUnionU_distrib_of_transport`) is fully proved,
`propext`-only. The three headline theorems — `interUnionDistrib_of_tabular`,
`interUnionDistrib_of_semiSimple`, `exists_unionOfSemiSimple_not_distributive` — are
themselves sorry-free statements; (a)/(b) delegate to the two gap lemmas, (c) is fully proved.
Builds green via `lake build Fredy.S2_22`.
