# §1.95–§1.96 Tracker — `Fredy/S1_95.lean`

## Status legend
| Status  | Meaning                                                         |
| ------- | --------------------------------------------------------------- |
| DONE    | Faithful statement, complete proof, zero sorry                  |
| SORRY   | Faithful statement, proof is `sorry`                            |
| DEF     | Definition / typeclass, no proof needed                         |
| MISSING | Cannot be faithfully stated yet (noted reason); skipped per rule |

---

## §1.95  A topos is a pre-topos

| §     | Decl                              | Status | Notes                                                                        |
| ----- | --------------------------------- | ------ | ---------------------------------------------------------------------------- |
| 1.951 | `graphComp_le_level`              | DONE   | (private) `(graph g)⊚(graph g)° ⊂ level g`; re-proved locally (S1_64 ver is private). |
| 1.951 | `level_le_graphComp`              | DONE   | (private) `level g ⊂ (graph g)⊚(graph g)°`; re-proved locally. |
| 1.951 | `effective_of_quotient_cover`     | DONE   | **NEW, sorry-free recovery half of §1.951/568**: E equiv rel + cover x:A→Q with `level x ≅ E` ⟹ `IsEffective E`. Packages `E≅level x≅(graph x)⊚(graph x)°` via the two bridges. |
| 1.951 | `topos_is_effective`              | SORRY  | Instance: Topos → EffectiveRegular. **Reduced**: recovery half proved (`effective_of_quotient_cover`); residual gap = quotient-cover existence (power-object A→Ω^A) on top of §1.54-blocked `HasImages`/`PullbacksTransferCovers`. |
| 1.952 | `topos_is_positive`               | SORRY  | Instance: Topos → HasBinaryCoproducts. Power-object construction is deep; **`rtc` is NOT the relevant input** (positivity is the ΔR/singleton-map power-object route, §1.92-blocked). |
| 1.954 | `equivalenceRelation_of_isEquivRel` | DONE | **NEW, axiom-free** bridge §1.77↔§1.56: `IsEquivRel E` (relation-algebra refl/sym/trans) ⟹ `EquivalenceRelation E` (§1.56 section-witness form). `#print axioms`: none. |
| 1.954 | `minEquiv_of_rtc`                 | DONE   | **NEW, sorry-free** (`propext, Classical.choice` only). `[HasReflTransClosure 𝒞]` ⟹ `HasMinEquivContaining 𝒞`: the minimal equiv containing `R` is the equivalence closure `(R ∪ R° ∪ 1)*`, built by `equivClos_from_symm_transRefClos` from the symmetrisation `Rsym=(R∪ᵣR°)∪ᵣ graph 1` and `rtc Rsym`. |
| 1.954 | `preTopos_rtc_has_coequalizers`   | DONE   | **NEW, sorry-free** (`propext, Classical.choice`; NO `sorryAx`). `[PreTopos 𝒞] [HasReflTransClosure 𝒞]` ⟹ `HasCoequalizers 𝒞`, via `minEquiv_of_rtc` + §1.657 `preTopos_minEquiv_to_cocartesian`. This is the **substantive content** of §1.954. |
| 1.954 | `topos_has_coequalizers`          | SORRY  | Instance: Topos → HasCoequalizers. **Reduced**: substance fully proved in `preTopos_rtc_has_coequalizers`; residual = synthesising `[PreTopos]` (= §1.951 `topos_is_effective`, §1.54-blocked) and a `[HasReflTransClosure]` *instance* on a bare topos (§1.543 capitalization). |
| 1.955 | `topos_is_bicartesian`            | SORRY  | Instance: Topos → BicartesianCategory. Follows from §1.952, §1.954, §1.944. |

---

## §1.96  Miscellaneous aspects of topoi

### §1.961  Injective objects

| §     | Decl                              | Status | Notes                                                                          |
| ----- | --------------------------------- | ------ | ------------------------------------------------------------------------------ |
| 1.961 | `IsInjective`                     | DEF    | (-, E) carries monics to epics; elementary: monic E ↣ A has right-inverse.    |
| 1.961 | `mono_comp`                       | DONE   | (private) composite of two monics is monic.                                    |
| 1.961 | `omega_is_injective`              | DONE   | Ω is EXTERNALLY injective: maps A→Ω extend along monics A↣B via `classify`.    |
| 1.961 | `prodMapLeft`                     | DEF    | Helper: A × Z → B × Z for f : A → B.                                          |
| 1.961 | `expMap`                          | DEF    | Contravariant E^f : E^^B → E^^A induced by f : A → B.                         |
| 1.961 | `IsInternallyInjective`           | DEF    | E^(−) carries monics to epics.                                                 |
| 1.961 | `cover_of_section`                | DONE   | **NEW, axiom-free**: split epi (`s≫e=1`) is a cover. The "Ω^f has section ⟹ cover" step. |
| 1.961 | `expMap_omega_eq_omegaPow`        | DONE   | **NEW, `rfl`**: `expMap Ω f = omegaPowContra.map f`. DRY bridge §1.961↔§1.922 (reuse proved Ω^(−) functoriality). |
| 1.961 | `omega_is_internally_injective`   | SORRY  | Ω is INTERNALLY injective. **Reduced**: section-⟹-cover proved (`cover_of_section`) + bridge to `Ω^f` proved; residual gap = the section `powerMapCov f` (§1.92 keystone). |

### §1.962  Ω^A is injective

| §     | Decl                                        | Status | Notes                                                    |
| ----- | ------------------------------------------- | ------ | -------------------------------------------------------- |
| 1.962 | `prodMap_mono`                              | DONE   | (private) right-factor A×f monic when f monic.          |
| 1.962 | `curry_precomp_exp`                         | DONE   | (private) transpose naturality f≫curry k = curry(A×f≫k). |
| 1.962 | `exp_of_injective_is_injective`             | DONE   | E injective → E^A injective via (-, E^A) ≅ (- × A, E). |
| 1.962 | `topos_every_object_embeds_in_injective`    | DONE   | Every A ↣ Ω^A (singleton map); Ω^A is injective.        |

### §1.963  2 is injective

MISSING: §1.963 says "2 is injective" (where 2 is the object from §1.934, the two-element subobject
classifier for decidable propositions). We do not have `2` (the boolean subobject classifier) defined
in this codebase (§1.934 not yet formalized). Statement skipped.

### §1.964  Value-based categories

| §     | Decl                                        | Status | Notes                                                        |
| ----- | ------------------------------------------- | ------ | ------------------------------------------------------------ |
| 1.964 | `IsValueBased`                              | DEF    | Category where subterminators form a basis (§1.632).         |
| 1.964 | `omega_cogenerates_in_value_based_topos`    | DONE   | **NEW, sorry-free.** Ω cogenerates in a value-based topos. Avoids Freyd's image route (needs §1.54-blocked `HasImages`): a value `x:U→A` out of a subterminator makes `x≫f` monic, so `h := χ(x≫f)` separates — via `monic_is_equalizer` (§1.913) + subterminal collapse `k=id`, the no-separation hypothesis forces `x≫f = x≫g` for every value, so `IsValueBased` ⟹ `f=g`. Axioms: propext, choice, Quot.sound. |

### §1.965  Internally cogenerates

| §     | Decl                              | Status | Notes                                                       |
| ----- | --------------------------------- | ------ | ----------------------------------------------------------- |
| 1.965 | `InternallyCogenerates`           | DEF    | C^(−) is a contravariant embedding.                         |
| 1.965 | `cogenerator_internally_cogenerates` | DONE  | Cogenerator → internally cogenerates. Added `[HasTerminal 𝒞]`. Proof: curry_inj, point specialisation at `pair fstA (sndA ≫ curry(fstB≫h))`, factor via B×one, curry_eval_eq, fst monic.  |
| 1.965 | `omega_internally_cogenerates`    | SORRY  | Ω internally cogenerates. Needs faithful representation.    |

### §1.966  Progenitor

| §     | Decl                                        | Status | Notes                                                          |
| ----- | ------------------------------------------- | ------ | -------------------------------------------------------------- |
| 1.966 | `IsProgenitor`                              | DEF    | G is a progenitor: subobjects of G form a generating set.      |
| 1.966 | `topos_value_based_iff_terminal_progenitor` | DONE   | `Iff.rfl`: both sides unfold to `IsGeneratingSet (fun X => ∃ m : X ⟶ one, Mono m)`. |
| 1.966 | `progenitor_omega_exp_cogenerates`          | SORRY  | G progenitor → Ω^G cogenerates. Uses exp-adjunction + §1.965. |

### §1.967  Powers ↔ copowers ↔ copowers of 1

| §     | Decl                                        | Status | Notes                                                                         |
| ----- | ------------------------------------------- | ------ | ----------------------------------------------------------------------------- |
| 1.967 | `HasArbitraryPowers`                        | DEF    | Class: I-fold power ∏_{i:I} A exists for all I, A.                           |
| 1.967 | `HasArbitraryCopowers`                      | DEF    | Class: I-fold copower ∐_{i:I} A exists for all I, A.                         |
| 1.967 | `LocallySmallTopos`                         | DEF    | Class: topos extending Topos (hom-sets small, same universe as index types).  |
| 1.967 | `topos_powers_copowers_equiv`               | SORRY  | (a)↔(b): arbitrary powers ↔ arbitrary copowers in a locally small topos.      |
| 1.967 | `topos_copowers_equiv_copowers_of_one`      | SORRY  | (b)↔(c): arbitrary copowers ↔ arbitrary copowers of 1.                        |
| 1.967 | `topos_powers_implies_locally_complete`     | SORRY  | (a) implies local completeness (equalizer of χ-maps).                         |

### §1.968  Complete ↔ cocomplete

| §     | Decl                                | Status | Notes                                                               |
| ----- | ----------------------------------- | ------ | ------------------------------------------------------------------- |
| 1.968 | `topos_complete_iff_cocomplete`     | SORRY  | Locally small topos is complete iff cocomplete. Uses §1.967.        |

### §1.969  Lawvere / Tierney definitions

| §     | Decl                                | Status | Notes                                                                    |
| ----- | ----------------------------------- | ------ | ------------------------------------------------------------------------ |
| 1.969 | `LawvereGrothendieckTopos`          | DEF    | Class: cocomplete topos with generating set.                             |
| 1.969 | `TierneyGrothendieckTopos`          | DEF    | Class: topos with progenitor and copowers of 1.                          |
| 1.969 | `lawvere_eq_tierney`                | SORRY  | Lawvere ↔ Tierney definition; uses §1.966 + §1.967.                     |

### §1.96(10)–§1.96(11)  Counterexamples and slice lemma

MISSING: §1.96(10) describes a concrete category A of set-actions and its full subcategory B.
This is a counterexample/example — it requires constructing specific concrete categories with
non-trivial set-theoretic content. Not formalizable without significant infrastructure.

MISSING: §1.96(11) (Slice lemma for Grothendieck topoi) is stated in §1.96 of the book
but its formalization belongs with §1.97 slice theory. It requires `Slice` category
infrastructure (already in `Fredy/Slice.lean`). Skipped here to avoid duplication.

---

## Sorries: justification summary

**Count: 12 own `sorry` (was 13).**  §1.964 `omega_cogenerates_in_value_based_topos` is now
DONE (sorry-free) — it only needs the classifier + `monic_is_equalizer`, not the §1.54-blocked
image machinery, because the value-based hypothesis is supplied directly.

The remaining 12 sorries correspond to the book's proofs which require one or more of:
- **Full topos machinery not yet built**: power-object ↔ coproduct constructions,
  image factorization in the power-object language (§1.951, §1.952, §1.954, §1.955).
- **Representation theorems**: §1.965 uses faithful embedding into a capital topos
  (§1.635), which is not yet in the repo.
- **Exponential/adjunction infrastructure**: §1.962, §1.966 use curry/uncurry
  naturality in ways not yet encoded.
- **Complete ↔ cocomplete**: §1.967, §1.968 are equivalent-conditions theorems
  whose proof is a multi-step chain through power/copower constructions; each step
  is non-trivial and relies on the above missing pieces.

## Sharpened blocker for the §1.967/§1.968 power↔copower targets

The task singled out `topos_powers_copowers_equiv` (a↔b),
`topos_copowers_equiv_copowers_of_one` (b↔c), and `topos_complete_iff_cocomplete`
(§1.968) as candidates that "may follow from the power/copower universal properties
plus the topos exponential adjunction." After investigation they remain genuinely
blocked, not by missing UP plumbing but by two concrete upstream gaps:

1. **`Fredy/S1_92.lean:33 topos_has_exponentials` is itself `sorry`.** `Topos`
   (Fredy/S1_9.lean:144) extends only `HasTerminal`, `HasBinaryProducts`,
   `HasSubobjectClassifier` — it does NOT carry a `HasExponentials` instance, and
   the one that would supply it (`topos_has_exponentials`) is unproven (it needs the
   §1.859 `baseable_inclusion_preserves_equilizers` fact, also `sorry`). So the
   "exponential adjunction" the hint relies on (power of A = copower transposed) is
   not available as usable `curry`/`eval` data inside these theorems.

2. **No distributivity / complemented-injection structure.** Even granting
   exponentials, both directions need a *construction*, not just a UP rearrangement:
   - (a)→(b) builds `∐ᵢ A` as a subobject of `∏ᵢ (A+1)` via complemented injections
     `uᵢuᵢ°=1, uᵢuⱼ°=0` — this needs §1.952 positivity (`topos_is_positive`, sorry)
     and image factorization.
   - (c)→(b) needs `∐ᵢ A ≅ (∐ᵢ 1) × A`, i.e. products distribute over the I-indexed
     coproduct — there is no distributivity lemma in scope, and the hypotheses give
     only bare `HasBinaryProducts`/`HasBinaryCoproducts`, not `HasAllCoproducts`.
   Each iff needs BOTH directions, so neither the trivial (b)→(c) specialisation nor
   any single UP step suffices; faking the iff with one half would be dishonest.

3. **§1.965 `cogenerator_internally_cogenerates` / `omega_internally_cogenerates`.**
   The faithful-cogenerator argument (T(C^f) ≠ T(C^g) for points T = (one,−))
   requires the unitor `prod one B ≅ B` and the transpose-of-points natural bijection
   `(one, C^^B) ≅ (B, C)`. Neither the unitor nor that naturality is in the repo
   (S1_85 has no `prod one` iso, no point-naming infrastructure). Building them is a
   separate infrastructure task, out of scope for this file.

## Sharpened blocker for `omega_is_internally_injective` (§1.961)

The remaining §1.961-962 injective sorry is **internal** injectivity of Ω
(`omega_is_internally_injective : Cover (expMap Ω f)` for monic `f`), as distinct from
the now-proved **external** injectivity (`omega_is_injective`). These are genuinely
different statements: external says hom-sets `(−,Ω)` carry monics to epis; internal says
the *exponential transpose* `Ω^f : Ω^B → Ω^A` is a cover. External injectivity does NOT
imply internal — the book's §1.961 proof of internal injectivity is a separate argument.

Freyd's argument: `Ω^f = [f"]` (post-compose-by-`f°`, i.e. the contravariant power map),
which has a left-inverse `[f]` because `f"f = 1` (f monic). A map with a section is a
cover (provable here: `s ≫ φ = id` plus `monic_cover_iso`-style retraction-is-iso). The
single missing ingredient is the section itself: the **covariant direct-image power map
`[f] = powerMapCov f`** (`Fredy/S1_92.lean:247`) is an unfilled `sorry`, because it needs
the §1.56 regular image factorization packaged as the `∃_f ⊣ f#` adjunction on power
objects (the direct image of the universal membership relation pushed along f). Until
`powerMapCov` and the identity `expMap Ω f ≫ powerMapCov f = id` (from `f"f=1`) are
available, the section cannot be produced. The faithful statement is in place; the proof
is `sorry` pending `powerMapCov`.

DISCHARGED across sessions:
- §1.961 `omega_is_injective` — Ω is externally injective. Constructive proof from the
  subobject-classifier universal property: given monic `f:A↣B` and `g:A→Ω`, let `m:S↣A`
  be the subobject `g` names (pullback of `true`); then `f ≫ classify(m≫f) = g` because
  `f` monic makes the pullback of `m≫f` along `f` equal to `m`, and `classify` is unique.
- §1.962 `exp_of_injective_is_injective` — `E` injective ⟹ `E^A` injective via
  uncurry/extend/recurry. Transpose naturality `curry_precomp_exp` closes it.
- §1.962 `topos_every_object_embeds_in_injective` — every object embeds in `Ω^A`.
- §1.966 `topos_value_based_iff_terminal_progenitor` via `Iff.rfl` (definitional).
- §1.965 `cogenerator_internally_cogenerates` (this session) — added `[HasTerminal 𝒞]`
  to signature (the book's proof implicitly uses global points T = Hom(1,−)).
  Proof: `curry_inj` gives the uncurried equation; specialise at the "point map"
  `s = pair fstA (sndA ≫ curry(fstB≫h)) : prod A one → prod A (C^^B)`; reduce via
  pair factorisation through `prod B one` and `curry_eval_eq`; cancel the monic `fst`
  (retraction `prodOneRightInv`) to get `f≫h = g≫h`, contradicting the cogenerator.

None of these sorries are vacuous: every statement faithfully reflects the book.

## Session re-audit (this pass): all 13 sorries remain genuinely blocked

Re-verified every blocker by instance-synthesis probing (`lean_multi_attempt`) against a live
`[Topos 𝒞]` context. Findings:

- `HasEqualizers`, `HasImages`, `RegularCategory`, `EffectiveRegular`, `HasBinaryCoproducts`,
  `HasExponentials` **do** synthesize for `[Topos 𝒞]` — but ONLY transitively through the
  `sorry`'d instances in this file (`topos_is_effective`, `topos_is_positive`) and the
  `sorry`'d `topos_has_exponentials` (S1_92:35, body `sorry` at S1_92:59). Building anything on
  them merely inherits the upstream `sorry` — no real discharge. (Skill rule: distinguish an
  inherited `sorryAx` from a real proof.)
- `HasCoterminator 𝒞` does **NOT** synthesize — the §1.944 initial object is genuinely absent.
  This is the *sole extra* blocker for `topos_is_bicartesian` beyond the §1.951/952/954 chain
  (which is itself a `sorry`-chain rooted at §1.951 regular effectiveness).

Per-target irreducible blocker (each lives outside the editable file `S1_95.lean`):

| Decl | Root blocker (not editable here) |
| ---- | -------------------------------- |
| `topos_is_effective` §1.951 | regular effectiveness of a topos: construct `IsEffective E` for every equiv rel via power-object/[A]-tabulation quotient. No upstream theorem produces it. |
| `topos_is_positive` §1.952 | binary coproducts via power-object subobject construction; chains on §1.951. `rtc` is unrelated. |
| `topos_has_coequalizers` §1.954 | substance PROVED (`preTopos_rtc_has_coequalizers`, sorry-free); residual = `[PreTopos]` (§1.951, §1.54-blocked) + a `[HasReflTransClosure]` instance on a bare topos (§1.543). |
| `topos_is_bicartesian` §1.955 | needs `HasCoterminator` (§1.944, absent) + the §1.951/952/954 chain. |
| `omega_is_internally_injective` §1.961 | `powerMapCov` (S1_92:267) is `sorry`; section `Ω^f ≫ [f] = id` from `f"f=1` unavailable. |
| `omega_cogenerates_in_value_based_topos` §1.964 | image+classify present, but no `classify`↔factorization bridge lemma; the subterminator-witness → χ(Im xf) argument needs ~60-line image/χ infra not in repo. |
| `omega_internally_cogenerates` §1.965 | capitalization §1.543 (faithful embedding into value-based topos) — ABSENT from repo. |
| `progenitor_omega_exp_cogenerates` §1.966 | exp-adjunction `(−,Ω^G) ≅ (G,Ω^−)` + §1.965 (above). |
| `topos_powers_copowers_equiv` §1.967 | `topos_has_exponentials` `sorry` + complemented-injection / distributivity construction. |
| `topos_copowers_equiv_copowers_of_one` §1.967 | distributivity `∐ᵢA ≅ (∐ᵢ1)×A`; no distributivity lemma in scope. |
| `topos_powers_implies_locally_complete` §1.967 | equalizer-of-χ-maps + well-poweredness → arbitrary unions; deep. |
| `topos_complete_iff_cocomplete` §1.968 | chains on all of §1.967. |
| `lawvere_eq_tierney` §1.969 | chains on §1.966 + §1.967. |

Honest verdict: **0 of 13 dischargeable by editing only `S1_95.lean`.** Every faithful proof
requires a proof-or-construction in an upstream file (`S1_92` exponentials/`powerMapCov`,
§1.944 coterminator, §1.951 effectiveness, §1.543 capitalization). Faking any would inherit an
upstream `sorry` or fabricate absent structure — both forbidden by the integrity rules.

## This pass (keystone reduction, no new sorries)

Rather than fake the two whole-instance sorries, this pass split off and **proved** the
parts of each keystone that are genuinely provable from existing infrastructure, leaving a
single sharp residual gap each. Sorry count UNCHANGED (13 in S1_95, 5 in S1_92); five new
sorry-free declarations added.

**Keystone (1) `topos_is_effective` (§1.951).** The substantive *recovery half* is now
PROVED as `effective_of_quotient_cover`: given an equivalence relation `E` and a cover
`x : A → Q` whose level (kernel pair) equals `E` (`E ⊂ level x ∧ level x ⊂ E`), then
`IsEffective E` holds. The proof packages `E ≅ level x ≅ (graph x) ⊚ (graph x)°` using two
locally-re-proved bridges (`graphComp_le_level`, `level_le_graphComp` — the S1_64 originals
are `private`). `cover_of_section` is axiom-free; `effective_of_quotient_cover` uses only the
`Classical.choice` already inherent in the repo's `image` API. **Residual gap (sharp):** the
*quotient-cover existence* — for each equiv rel `E`, a cover `q : A ↠ A/E` with `level q ≅ E`
(Freyd's power-object construction `A → Ω^A`), plus `HasImages`/`PullbacksTransferCovers`
from a bare topos — all resting on §1.54 capitalization (`sorry`) and the opaque exponential.

**Keystone (2) `omega_is_internally_injective` (§1.961), unblocking the §1.92 `powerMapCov`
story.** Two sorry-free lemmas now reduce it to exactly `powerMapCov`:
- `cover_of_section`: a split epi is a cover (axiom-free).
- `expMap_omega_eq_omegaPow`: `expMap Ω f = omegaPowContra.map f` by `rfl`, identifying the
  §1.961 contravariant action with the **proved** §1.922 functor `Ω^(−)`.
So once `powerMapCov f : Ω^A → Ω^B` and its identity `expMap Ω f ≫ powerMapCov f = id`
(from `f"f = 1`) are available, `omega_is_internally_injective` is
`cover_of_section (expMap Ω f) (powerMapCov f) <identity>`. The residual gap is precisely
`powerMapCov` (S1_92), itself blocked on the power-object membership of the opaque `exp A Ω`.

## This pass (§1.954 coequalizers — substantive reduction discharged via `rtc`)

`Fredy/S1_77.lean` now supplies `HasReflTransClosure` / `rtc` / `equivClos_from_symm_transRefClos`.
That closes the *equivalence-closure* sub-problem of §1.954, so the coequalizer reduction is now
fully proved as a standalone theorem (no `sorry`, no `sorryAx`):

- **`equivalenceRelation_of_isEquivRel`** (axiom-free) — bridges the relation-algebra notion
  `IsEquivRel E` (`1⊑E`, `E°⊑E`, `E⊚E⊑E`, §1.77) to the §1.56 `EquivalenceRelation E`
  (reflexivity *witness* + `RelHom E E°` + `RelHom (E⊚E) E`). Symmetry is reciprocal-monotonicity
  + involution; reflexivity unfolds `RelHom (graph 1_A) E`; transitivity is literal.
- **`minEquiv_of_rtc`** (`propext, Classical.choice`) — `[HasReflTransClosure 𝒞]` ⟹
  `HasMinEquivContaining 𝒞`. The minimal equivalence containing `R` is the equivalence closure
  `(R ∪ R° ∪ 1)*`: form `Rsym := (R ∪ᵣ R°) ∪ᵣ graph 1_A` (the join, hence `hJoin = le_relUnion`),
  prove `IsSymmetric Rsym` via `Rsym ⊑ Rsym°` + reciprocal involution, take `rtc Rsym`, and feed
  both to `equivClos_from_symm_transRefClos`. The `EquivClos.minimal` field is transported to
  `HasMinEquivContaining`'s minimality through the bridge above.
- **`preTopos_rtc_has_coequalizers`** (`propext, Classical.choice`; **no `sorryAx`**) —
  `[PreTopos 𝒞] [HasReflTransClosure 𝒞]` ⟹ `HasCoequalizers 𝒞`, by `minEquiv_of_rtc` composed
  with §1.657 `preTopos_minEquiv_to_cocartesian` (effective-regular route: minimal equiv `S ⊇ R`
  is the level of a cover `q`, and `q` coequalizes `f,g`). This is the FULL substantive content
  of §1.954, verified `#print axioms`-clean of `sorryAx`.

**Honest assessment of the bare-topos gap.** `rtc` + `effective_of_quotient_cover` reduce §1.954
to: (1) a `[PreTopos]` instance (i.e. `topos_is_effective`, §1.951, blocked on §1.54
capitalization), and (2) a `[HasReflTransClosure]` *instance* on a bare topos — which §1.94's
`topos_has_rtc` documents as itself resting on the §1.943 family-glb (§1.543 capitalization). So
`rtc` narrows the blocker exactly as expected: the equivalence-closure construction is no longer
the gap; the residual is the §1.54/§1.543 *instance existence* of effectiveness and rtc on a
topos. `topos_has_coequalizers` stays a sharp faithful `sorry` pinning (1)+(2); its body would be
literally `preTopos_rtc_has_coequalizers` once both instances exist.

Implementation note: under `[PreTopos 𝒞]`, `HasBinaryProducts 𝒞` has two non-syntactically-equal
(but defeq) instances — `PositivePreLogos.toHasBinaryProducts` and the `Topos`-gated
`topos_has_exponentials.toHasBinaryProducts` (the latter's `Topos 𝒞` premise is in fact
unsynthesizable here, yet instance search still mis-offers it). `preTopos_rtc_has_coequalizers`
pins every instance of `minEquiv_of_rtc` to the canonical `PreTopos → PositivePreLogos`
projections (and states its `HasReflTransClosure` hypothesis over them) so instance-implicit
unification with `preTopos_minEquiv_to_cocartesian` succeeds.

§1.947 / `topos_is_positive` re-check: `topos_has_rtc` (S1_94, out of edit scope) already consumes
`rtc` abstractly and is discharged modulo §1.543; `topos_is_positive` (binary coproducts) is the
power-object/singleton-map construction and does **not** use `rtc`, so there is nothing to feed
there. Both confirmed.
