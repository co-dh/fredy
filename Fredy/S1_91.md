# Fredy/S1_91.lean — sorry status (§1.91 topos structure)

One honest `sorry` remains (`omega_monic_endo_is_involution`, §1.919). The §1.91(10) terminator
theorem, previously a vacuous-or-blocked `sorry`, is now **fully proven** under a faithful (terminator-free)
hypothesis. Nothing is faked: `#print axioms minimal_topos_has_terminator = [Classical.choice]` (no
`sorryAx`, no custom axiom; `Classical.choice` only from the existing `IsUniversalRel.classify_exists`
∃-elimination, i.e. `powerClassify := Exists.choose`).

## 1. `omega_monic_endo_is_involution` (§1.919) — BLOCKED

Claim: a monic `g : Ω → Ω` satisfies `g ≫ g = id`.

- The old docstring blamed a missing `classify_unique`. That is wrong: `HasSubobjectClassifier`
  (Fredy/S1_9.lean:137) already provides `classify_unique`. Docstring corrected.
- The real gap is the **subobject algebra** Freyd's proof runs on:
  - the g-large-subobject relation (`A' ⊆ A` is g-large iff `g(A') = A`),
  - the equational theory of `omegaMeet` (∧) and `heytingDoubleArrow` (⇔). Both are only *defined*
    in this file (lines ~125–139) with **no proven laws**.
  - the bijection "maps `A → Ω` ↔ subobjects of `A`" used to move between `g²(A) = A` for all `A`
    and the single equation `g ≫ g = id`.
- `apply hm` (mono-cancel) reduces the goal only to `g ≫ g ≫ g = g` (`g³ = g`), still needing the
  same algebra. No tactic shortcut (`simp`, `aesop`) makes progress.
- Removable once §1.914's ∧/⇔ get their equational laws and the subobject↔characteristic-map
  correspondence over an arbitrary object is formalized.

## 2. `minimal_topos_has_terminator` (§1.91(10)) — PROVEN (statement defect repaired)

Claim (Freyd §1.91(10)): a non-empty category with binary products, equalizers, pullbacks, and a
power-object for **every** object — but **NO** assumed terminator — already has a terminator.

### The repaired statement

The old statement bundled `[HasSubobjectClassifier 𝒞]`, which `extends HasTerminal`, so a terminator was
already in the context: `exact ⟨inferInstance⟩` type-checked but proved nothing (vacuous), exactly the
trap §1.91(10) warns against. The hypothesis is now the terminator-free data of Freyd's construction:

```
[HasPullbacks 𝒞] [HasBinaryProducts 𝒞] [HasEqualizers 𝒞] [∀ C : 𝒞, HasPowerObject C] (B : 𝒞)
    ⟹ Nonempty (HasTerminal 𝒞)
```

`HasPowerObject` (Fredy/S1_9.lean:101) needs only `HasPullbacks` — it does **not** presuppose a terminal
object — so the conclusion is non-trivial. `B` witnesses non-emptiness.

### The construction (all proven, no new sorry)

- `fullRel A C : BinRel 𝒞 A C` — the "full" relation `M_{A,C}` tabulated by the product projection
  (table `A×C`, columns `fst`, `snd`); jointly monic via `fst_snd_jointly_monic`.
- `fullRel_pullback : f(M_{A,C}) ≅ M_{A',C}` — pulling the full relation back along `f : A' → A` is
  again full; the pullback of `fst` along `f` is `A'×C` (built with `pair`), iso of tables both ways.
- `powerClassify_natural : Λ(relPullback g R) = g ≫ Λ(R)` — naturality of the classifying map, from
  `relPullback_comp` (this file) + universality's `classify_unique`. (S1_92 has the same lemma as
  `univClassify_natural`, but S1_92 imports S1_91, so it is reproven locally here — the one forced
  duplication; `relHom_trans`/`relHom_pullback` likewise.)
- `fullClassify_const : g ≫ Λ(M_{A,B}) = Λ(M_{X,B})` — constancy of `Λ(M_{−,B})`: naturality +
  `fullRel_pullback`.
- Terminator: `e := Λ(M_{[B],B}) : [B] → [B]` is constant; `T := equalizer (id_{[B]}, e)`.
  - `trm A`: `Λ(M_{A,B})` equalizes `id`/`e` (since `Λ(M_{A,B}) ≫ e = Λ(M_{A,B})` by constancy), so it
    factors through `T`.
  - `uniq`: for `u, v : A → T`, constancy gives `(u≫eqMap)≫e = (v≫eqMap)≫e`, and `eqMap ≫ e = eqMap`
    (equalizer relation), so `u≫eqMap = v≫eqMap`; the equalizer map is monic, hence `u = v`.

## 1.919 sibling-file survey (no reusable lemma found)
- S1_92: `singletonMapCat`, `powerMapCov`, `expSubobj`, `heytingImpl` on `SubTerminal` — none give the
  Ω-algebra laws or the subobject↔map bijection over arbitrary objects.
- S1_84/S1_85: covers-as-set, replete subcategories — unrelated to Ω involutions.
