# §1.97–§1.98 Tracker — Fredy/S1_97.lean

## Status

Build: **green** (zero `error:`, all sorries are faithful stubs)

## Declarations

| Declaration | Section | Status |
|---|---|---|
| `BooleanTopos` | §1.97 | done (class) |
| `HasNaturalNumbersObject` | §1.98 | done (class) |
| `expPostMap` | §1.973 | done |
| `IsIAC` | §1.973 | done |
| `ac_iff_iac_and_projective_one` | §1.974 | **PROVED** — split-epi/cover + exp functoriality + pullback-transfers-covers hyp |
| `iteratePair` | §1.981 | done (def, via curry/exponential adjunction) |
| `iteratePair_zero` | §1.981 | **PROVED** — curry adjunction + prodMap collapse, axiom-free |
| `iteratePair_succ` | §1.981 | **PROVED** — prodMap_comp + iterate_succ + expPostMap eval, axiom-free |
| `iteratePair_unique` | §1.981 | **PROVED** — transpose `curry h`, NNO `iterate_unique`, axiom-free |
| `primRec` | §1.983 | done (def, via iteratePair of triples) |
| `primRec_fst` | §1.983 | **PROVED** — carrier identity by §1.981 uniqueness |
| `primRec_zero` | §1.983 | **PROVED** — iteratePair_zero + snd_pair |
| `primRec_succ` | §1.983 | **PROVED** — k = ⟨p₁,f⟩ via primRec_fst, iteratePair_succ |
| `primRec_unique` | §1.983 | **PROVED** — ⟨p₁,p₂,f⟩ iterates (a',b'), §1.981 uniqueness |
| `nno_is_coproduct` | §1.985 | **PROVED** — inverse iterate inl `[0≫inr,s≫inr]`, NNO uniqueness |
| `nno_terminal_is_coequalizer` | §1.985 | **PROVED** — NNO uniqueness + zero∘term=id |
| `PeanoProperty` | §1.987 | done (def) |
| `nno_peano_property` | §1.987 | **PROVED** — B-algebra iterate is a section of B.arr; mono+section ⇒ iso |
| `least_peano_subobject` | §1.987 | **sorry** — §1.543 family-glb name (see below) |
| `nno_of_bicartesian_data` | §1.98(10) | **sorry** — missing `HasPartialMapClassifier` laws (§1.988) |
| `bicartesian_functor_preserves_nno` | §1.98(11) | reduces to §1.98(10) (`exact nno_of_bicartesian_data …`); blocked by it |
| `AAction` | §1.98(12) | done (structure) |
| `FreeAAction` | §1.98(12) | done (structure) |
| `nno_is_free_one_action` | §1.98(12) | **PROVED** — N is the free 1-action; iterate + prodMap_factorN + snd section |
| `free_action_iff_bicartesian` | §1.98(13) | **sorry** — A-parametrised §1.98(10) (§1.988) |
| `free_action_exists` | §1.98(14) | **sorry** — list object Σₙ Aⁿ / W-type (see below) |

## Remaining sorries (4) — all genuinely blocked on absent infrastructure

Every other declaration above is proved (the §1.981 NNO-iterate-for-pairs cluster,
§1.983 primitive recursion, §1.985 coproduct+coequalizer, §1.987 Peano property,
§1.974 AC↔IAC, §1.98(12) free 1-action are all sorry-free). The 4 stubs below cannot be
closed without machinery this repo does not yet have; faking them would violate the
verbatim-statement / no-sorry-under-changed-statement integrity rule.

- **least_peano_subobject** (§1.987, line ~655). Constructive *except* for one input:
  the global NAME `F_name : 1 → Ω^A` of the least `(a,t)`-closed subobject, i.e. the
  glb `⋂{ S ↣ A | a∈S ∧ t(S)⊆S }`. `S1_94.interIntersection` only collapses a *singleton*
  name `1 → Ω^A`, not a family glb over a subobject-family; the family glb needs the
  internal-∀ comprehension `Φ = {G | a∈G ∧ ∀x, x∈G→tx∈G}` on Ω^A plus the §1.543
  capitalization lemma (`S1_94` itself flags this as `sorry`). The proof bundles the
  missing input as `closedData` and discharges leastness via `inter_le_named`; the
  single `sorry` is exactly that §1.543 family-glb name. **Blocked on §1.543.**

- **nno_of_bicartesian_data** (§1.98(10), line ~825). The converse of §1.985: build
  `iterate x f : A → X` from the bicartesian data via §1.988's partial-recursor graph,
  classified by the partial-map classifier `Ã`. `HasPartialMapClassifier` (S1_92) exposes
  only `pmc_obj`, `pmc_incl`(monic), and `pmc_classify` — a BARE map-former with NO
  equational laws. §1.988 needs (i) `pmc_restrict` (the classified map's defined-locus is
  the image of `m` and there agrees with the name of `f`) and (ii) `pmc_unique`. Neither
  is a field; no "graph of partial map" / "extend-along-monic" op is derivable from the
  three bare fields. So `iterate_zero`/`iterate_succ`/`iterate_unique` are unprovable from
  the interface. **Blocked on missing partial-map-classifier laws (§1.988).**

- **bicartesian_functor_preserves_nno** (§1.98(11)): is a literal `exact
  nno_of_bicartesian_data pmc' (tOne ≫ T 0) (T s) hT_iso hT_coeq` and so inherits the
  §1.98(10) blocker — it is NOT an independent sorry.

- **free_action_iff_bicartesian** (§1.98(13), line ~910). The A-parametrised analogue
  of §1.98(10): same §1.988 partial-map-classifier descent, now indexed by A.
  **Blocked on §1.988 (same as §1.98(10)).**

- **free_action_exists** (§1.98(14), line ~930). The free A-action object is the LIST
  object `A* = Σₙ Aⁿ` (finite words in A), NOT the NNO `N`: taking `A* := N` with
  `act := snd ≫ succ` yields only the free *1*-action (`nno_is_free_one_action`), whose
  `recA` cannot recover an A-action's dependence on the A-coordinate, so
  `recA_act`/`recA_uniq` fail for nontrivial A. Building `Σₙ Aⁿ` from a NNO needs an
  N-indexed coproduct / W-type for `X ↦ 1 + A×X` — beyond `iteratePair` (§1.981) and
  `primRec` (§1.983), which iterate a *fixed* fibre, not a growing power Aⁿ. No
  list-object / indexed-coproduct infrastructure exists in S1_85/S1_92/S1_94.
  **Blocked on the list-object (§1.98(14)) construction.**

## Proofs discharged

`nno_terminal_is_coequalizer` (§1.985): given f: N→X with succ≫f = f,
take g = zero≫f. Uniqueness: zero∘(term N) = id₁ by terminal uniqueness.
Existence: both f and (term N ≫ g) satisfy NNO equations for (g, id_X),
so NNO uniqueness gives f = term N ≫ g.

### §1.981 `iteratePair` equations (this session, all axiom-free / choice-free)

Let `a_hat = curry(fst≫a) : 1→B^A`, `b_hat = expPostMap A B B b : B^A→B^A`,
`H = iterate a_hat b_hat : N→B^A`. `iteratePair a b = prodMap A N (B^A) H ≫ eval`.

- **iteratePair_zero**: collapse `pair(id,term≫zero) ≫ prodMap H` into
  `pair(id, term≫zero≫H)`; `zero≫H = a_hat` (`iterate_zero`); recognise
  `pair(id,term≫a_hat) = pair(id,term) ≫ prodMap a_hat`; `≫eval = fst≫a`
  (`curry_eval_eq`), then `fst_pair`/`id_comp`.
- **iteratePair_succ**: `prodMap succ ≫ prodMap H = prodMap (succ≫H)`
  (`prodMap_comp`); `succ≫H = H≫b_hat` (`iterate_succ`); then
  `prodMap b_hat ≫ eval = eval ≫ b` (`curry_eval_eq`, since `b_hat=curry(eval≫b)`).
- **iteratePair_unique**: transpose `h` to `curry h : N→B^A`. Key local lemma
  `curry_precomp : g ≫ curry f = curry(prodMap g ≫ f)`. Show `curry h` solves the
  NNO recursion for `(a_hat,b_hat)` (zero eq uses `h0`+term-uniqueness, succ eq
  uses `hs`), so `iterate_unique` gives `curry h = H`; then `curry_eval_eq`
  yields `iteratePair a b = prodMap H ≫ eval = h`.
