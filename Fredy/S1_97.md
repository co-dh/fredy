# §1.97–§1.98 Tracker — Fredy/S1_97.lean

## Status

Build: **green** (zero `error:`, all sorries are faithful stubs)

## Declarations

| Declaration | Section | Status |
|---|---|---|
| `BooleanTopos` | §1.97 | done (class) |
| `HasNaturalNumbersObject` | §1.98 | done (class) |
| `expPostMap` | §1.973 | done |
| `IsIAC` | §1.973 | done |
| `ac_iff_iac_and_projective_one` | §1.974 | sorry — needs full AC/IAC infrastructure |
| `iteratePair` | §1.981 | done (def, via curry/exponential adjunction) |
| `iteratePair_zero` | §1.981 | **PROVED** — curry adjunction + prodMap collapse, axiom-free |
| `iteratePair_succ` | §1.981 | **PROVED** — prodMap_comp + iterate_succ + expPostMap eval, axiom-free |
| `iteratePair_unique` | §1.981 | **PROVED** — transpose `curry h`, NNO `iterate_unique`, axiom-free |
| `primRec` | §1.983 | sorry — construction via iteratePair of triples |
| `primRec_zero` | §1.983 | sorry — depends on primRec def |
| `primRec_succ` | §1.983 | sorry — depends on primRec def |
| `primRec_unique` | §1.983 | sorry — depends on primRec def |
| `nno_is_coproduct` | §1.985 | sorry — show [0,s]:1+N→N iso via NNO univ prop |
| `nno_terminal_is_coequalizer` | §1.985 | **PROVED** — NNO uniqueness + zero∘term=id |
| `PeanoProperty` | §1.987 | done (def) |
| `nno_peano_property` | §1.987 | sorry — needs §1.986 image/closure infrastructure |
| `least_peano_subobject` | §1.987 | sorry — needs reflexive-transitive closure (§1.947) |
| `nno_of_bicartesian_data` | §1.98(10) | sorry — needs Peano property (§1.988/2.542) |
| `bicartesian_functor_preserves_nno` | §1.98(11) | sorry — follows from §1.985+§1.98(10) |
| `AAction` | §1.98(12) | done (structure) |
| `FreeAAction` | §1.98(12) | done (structure) |
| `nno_is_free_one_action` | §1.98(12) | sorry — needs prodMap/iso for 1×N≅N |
| `free_action_iff_bicartesian` | §1.98(13) | sorry — analogue of §1.98(10) |
| `free_action_exists` | §1.98(14) | sorry — construction via primRec/NNO |

## Sorry reasons

- **primRec**: Definition requires building `k : A×N → A×N×B` via iteratePair,
  then projecting. Needs `pair`/`fst`/`snd` combinatorics for A×N×B.

- **nno_is_coproduct**: The inverse of `[0,s]` is built by NNO iteration
  sending `0 ↦ inl(*)` and `n ↦ inr(n-1)` — but encoding this requires the
  exponential adjunction or a direct iteration argument.

- **nno_peano_property**: Requires §1.986 infrastructure (reflexive-transitive
  closure of relations, §1.947) which is not yet in the repo.

- **least_peano_subobject**: Same dependency on §1.947 relational calculus.

- **nno_of_bicartesian_data / bicartesian_functor_preserves_nno**: The proof in
  the book cites [2.542] (boolean covers) and uses the Peano property from §1.988.

- **ac_iff_iac_and_projective_one**: Needs the pullback-along-f argument from §1.974
  plus well-supportedness infrastructure (§1.61).

- **nno_is_free_one_action / free_action_iff_bicartesian / free_action_exists**:
  Needs the 1×N≅N coherence isomorphism and primRec.

## Proofs discharged

`nno_terminal_is_coequalizer` (§1.985): given f: N→X with succ≫f = f,
take g = zero≫f. Uniqueness: zero∘(term N) = id₁ by terminal uniqueness.
Existence: both f and (term N ≫ g) satisfy NNO equations for (g, id_X),
so NNO uniqueness gives f = term N ≫ g.

### §1.981 `iteratePair` equations (this session, all axiom-free / choice-free)

Let `a_hat = curry(fst≫a) : 1→B^A`, `b_hat = expPostMap A B B b : B^A→B^A`,
`H = iterate a_hat b_hat : N→B^A`. `iteratePair a b = prodMap A N (B^A) H ≫ eval`.

- **iteratePair_zero**: collapse `pair(id,term≫zero) ≫ prodMap H` into
  `pair(id, term≫zero≫H)`; `zero≫H = a_hat` (`iterate_zero`); recognise
  `pair(id,term≫a_hat) = pair(id,term) ≫ prodMap a_hat`; `≫eval = fst≫a`
  (`curry_eval_eq`), then `fst_pair`/`id_comp`.
- **iteratePair_succ**: `prodMap succ ≫ prodMap H = prodMap (succ≫H)`
  (`prodMap_comp`); `succ≫H = H≫b_hat` (`iterate_succ`); then
  `prodMap b_hat ≫ eval = eval ≫ b` (`curry_eval_eq`, since `b_hat=curry(eval≫b)`).
- **iteratePair_unique**: transpose `h` to `curry h : N→B^A`. Key local lemma
  `curry_precomp : g ≫ curry f = curry(prodMap g ≫ f)`. Show `curry h` solves the
  NNO recursion for `(a_hat,b_hat)` (zero eq uses `h0`+term-uniqueness, succ eq
  uses `hs`), so `iterate_unique` gives `curry h = H`; then `curry_eval_eq`
  yields `iteratePair a b = prodMap H ≫ eval = h`.
