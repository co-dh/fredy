# §1.97–§1.98 Tracker — Fredy/S1_97.lean

## Status

Build: **green** (zero `error:`).  6 `sorry` tactics remain, all faithful stubs:
5 in §1.97–§1.98 scope (`least_peano_subobject`, `nno_of_bicartesian_data`,
`bicartesian_functor_preserves_nno`, `free_action_iff_bicartesian`, `free_action_exists`)
+ 1 out-of-scope (`ac_iff_iac_and_projective_one`, §1.974).
This session discharged `nno_is_coproduct` and `nno_peano_property`.

## Declarations

| Declaration | Section | Status |
|---|---|---|
| `BooleanTopos` | §1.97 | done (class) |
| `HasNaturalNumbersObject` | §1.98 | done (class) |
| `expPostMap` | §1.973 | done |
| `IsIAC` | §1.973 | done |
| `ac_iff_iac_and_projective_one` | §1.974 | sorry — needs full AC/IAC infrastructure |
| `pair_prodMap` | §1.981 | **PROVED** (helper: `⟨f,g⟩≫(A×h) = ⟨f,g≫h⟩`) |
| `prodMap_eval_inj` | §1.981 | **PROVED** (helper: `g↦(A×g)≫eval` injective, split by curry) |
| `iteratePair` | §1.981 | done (def, via curry/exponential adjunction) |
| `iteratePair_zero` | §1.981 | **PROVED** — absorb pair, iterate_zero, curry_eval |
| `iteratePair_succ` | §1.981 | **PROVED** — prodMap_comp, iterate_succ, expPostMap=curry(eval≫b) |
| `iteratePair_unique` | §1.981 | **PROVED** — transpose h via curry, NNO uniqueness, uncurry |
| `primRecBase`/`primRecStep` | §1.983 | done (def: a'=⟨⟨1,0⟩,g⟩, b'=⟨⟨p₁,p₂s⟩,h⟩) |
| `primRec` | §1.983 | done (def: `iteratePair a' b' ≫ snd`) |
| `primRec_fst` | §1.983 | **PROVED** (helper: k≫p₁ = 1, by §1.981 uniqueness) |
| `primRec_zero` | §1.983 | **PROVED** — iteratePair_zero + snd_pair |
| `primRec_succ` | §1.983 | **PROVED** — iteratePair_succ + k=⟨p₁,f⟩ |
| `primRec_unique` | §1.983 | **PROVED** — kf=⟨p₁,p₂,f⟩ satisfies §1.981 eqns; uniqueness |
| `nno_is_coproduct` | §1.985 | **PROVED** — NNO-iterate inverse, see below |
| `nno_terminal_is_coequalizer` | §1.985 | **PROVED** — NNO uniqueness + zero∘term=id |
| `PeanoProperty` | §1.987 | done (def) |
| `nno_peano_property` | §1.987 | **PROVED** — iterate gives a section of the inclusion, see below |
| `least_peano_subobject` | §1.987 | sorry — needs internal intersection / power object P(A) |
| `nno_of_bicartesian_data` | §1.98(10) | sorry — needs Peano property (§1.988/2.542) |
| `bicartesian_functor_preserves_nno` | §1.98(11) | sorry — follows from §1.985+§1.98(10) |
| `AAction` | §1.98(12) | done (structure) |
| `FreeAAction` | §1.98(12) | done (structure) |
| `nno_is_free_one_action` | §1.98(12) | **PROVED** — see proof strategy below |
| `free_action_iff_bicartesian` | §1.98(13) | sorry — analogue of §1.98(10) |
| `free_action_exists` | §1.98(14) | sorry — needs list-object construction |

## Remaining sorry reasons (all faithful; 5 in this file + 1 out-of-scope)

- **least_peano_subobject** (§1.987): `A'` is the *intersection* of all `(a,t)`-stable
  subobjects of `A`.  Building it (and its leastness/own-stability) needs the internal
  intersection / power-object `P(A) = Ω^A` machinery of a topos (a `∀` over `Sub(A)`).
  The repo exposes only `HasImages`, not arbitrary intersections, so this is not yet
  derivable here.

- **nno_of_bicartesian_data** (§1.98(10)): the CONVERSE of §1.985.  Producing the NNO
  instance means *constructing* `iterate x f : A → X` for arbitrary `(X,x,f)` from the
  iso `[a,t]:1+A≅A` and the coequalizer `A→1`, with existence+uniqueness.  Freyd's §1.988
  does this via the partial-map classifier (graph of the partial recursor, descended along
  the coequalizer) — infrastructure not present.

- **bicartesian_functor_preserves_nno** (§1.98(11)): reduces to `nno_of_bicartesian_data`
  applied to `(T N, T 0, T s)` (T preserves the bicartesian data by hypothesis; also needs
  `T1 ≅ 1` to retype the unit).  Blocked by the same §1.988 construction.

- **free_action_iff_bicartesian** (§1.98(13)): the A-parametrised analogue of §1.98(10);
  same partial-map-classifier / §1.988 descent, now for `[e,s]:1+A×A* ≅ A*`.

- **free_action_exists** (§1.98(14)): the free A-action object is the LIST OBJECT
  `A* = Σₙ Aⁿ`, **not** the NNO `N` (taking `A*:=N, act:=snd≫succ` gives only the free
  *1*-action — its `recA` cannot recover an A-action's dependence on the A-coordinate, so
  `recA_act`/`recA_uniq` fail for nontrivial A).  Building `Σₙ Aⁿ` needs a list-object /
  W-type construction (partial-map classifier for `X ↦ 1+A×X`), beyond the `iteratePair`
  (§1.981) and `primRec` (§1.983) lemmas available, which iterate a *fixed* fibre.

- **ac_iff_iac_and_projective_one** (§1.974, out of scope this session): needs the
  pullback-along-f argument plus well-supportedness infrastructure (§1.61).

## Proofs discharged this session

### `nno_is_coproduct` (§1.985(1)) — `[0,s] : 1+N → N` is iso

Let `c = [0,s] : 1+N → N`.  Build the inverse by NNO-iterate:
`f := [0≫inr, s≫inr] : 1+N → 1+N` and `d := iterate inl f : N → 1+N`.

Three case-uniqueness facts:
- `inl≫c = 0`, `inr≫c = s` (`case_inl`/`case_inr`).
- `f ≫ c = c ≫ s`: both legs equal `case (0≫s) (s≫s)` (apply `case_uniq` to each side).

**`d ≫ c = id_N`** by NNO-uniqueness (`id_N = iterate 0 s`):
`0≫d≫c = inl≫c = 0` ✓; `s≫d≫c = (d≫f)≫c = d≫(c≫s) = (d≫c)≫s` ✓.

**`c ≫ d = id_{1+N}`** by `case_uniq` (`id = case inl inr`):
- inl-leg: `inl≫c≫d = 0≫d = inl` ✓ (`iterate_zero`).
- inr-leg: `inr≫c≫d = s≫d = d≫f`, and `d≫f = inr` by a SECOND NNO-uniqueness —
  both `d≫f` and `inr` equal `iterate (0≫inr) f` (for `inr`: `s≫inr = inr≫f` is `case_inr`;
  for `d≫f`: `s≫d≫f = (d≫f)≫f`).  No circular "[0,s] monic" argument is needed.

### `nno_peano_property` (§1.987) — the NNO has the Peano property

Take a subobject `B ↣ N` allowing `0` (point `e:1→B.dom`, `e≫arr=0`) and stable under
`s` (`tB:B.dom→B.dom`, `tB≫arr = arr≫s`).  Then `(B.dom, e, tB)` is an N-algebra and
`sec := iterate e tB : N → B.dom` is a **section** of `arr`:
`sec≫arr = iterate 0 s = id_N` by NNO-uniqueness (`0≫sec≫arr = e≫arr = 0`;
`s≫sec≫arr = (sec≫tB)≫arr = sec≫arr≫s`).  Since `arr` is monic, a section is a
two-sided inverse: `arr≫sec = id_{B.dom}` by mono-cancelling `arr` from
`(arr≫sec)≫arr = arr≫(sec≫arr) = arr = id≫arr`.  Hence `IsIso arr`, i.e. `B` is entire.

### `nno_is_free_one_action` (§1.98(12))

**Construction.** The NNO (N, 0, s) is a free 1-action with:
- `obj = N`, `unit = zero`, `act = snd ≫ succ : 1 × N → N`
- `recA α := hN.iterate α.unit (pair(term α.obj)(Cat.id α.obj) ≫ α.act)` = NNO iterate
  with step `f_rec α := pair(term,id) ≫ α.act : α.obj → α.obj` ("apply α.act to (*, x)").

**Key identity** (helper `prodMap_factorN`):
`prodMap one N B h = (snd ≫ h) ≫ pair(term B)(Cat.id B)`
proved by `pair_uniq`: fst component = `term` (by `term_uniq`), snd component = `snd ≫ h` ✓.

**recA_unit**: direct from `hN.iterate_zero`.

**recA_act**: `prodMap one N B iter ≫ α.act`
= `((snd ≫ iter) ≫ pair(term,id)) ≫ α.act` [prodMap_factorN]
= `snd ≫ iter ≫ (pair(term,id) ≫ α.act)` [Cat.assoc × 3, congr 1]
= `snd ≫ succ ≫ iter` [← iterate_succ with explicit args]
= `(snd ≫ succ) ≫ iter` [Cat.assoc].

**recA_uniq**: from hypothesis `hms : prodMap one N α.obj m ≫ α.act = (snd ≫ succ) ≫ m`,
applying `prodMap_factorN` and reassociating gives
`snd ≫ m ≫ f_rec α = snd ≫ succ ≫ m` (as maps `1 × N → α.obj`).
Cancel `snd` from the left using its section `prodOneLeftInv N` (which satisfies
`prodOneLeftInv N ≫ snd = id_N`, i.e., `snd` has a section, so is left-cancellable).
Conclude `m ≫ f_rec α = succ ≫ m`, then `hN.iterate_unique` closes the goal.

## Proofs discharged in prior sessions

### §1.981 (NNO iterate for pairs) and §1.983 (primitive recursion)

All of §1.981 and §1.983 are proved from the NNO universal property + exponential adjunction.
See prior version of this file for detailed proof strategies.

### `nno_terminal_is_coequalizer` (§1.985)

Given `f : N → X` with `succ ≫ f = f`, take `g = zero ≫ f`.
Uniqueness: `zero ≫ term N = id₁` by terminal uniqueness.
Existence: both `f` and `term N ≫ g` satisfy NNO equations for `(g, id_X)`,
so NNO uniqueness gives `f = term N ≫ g`.
