# §1.94  Tracker — Internally Defined Intersection/Union

Build: `lake build Freyd.S1_94` — **green** (zero errors; **4** sorries remain, all gated on §1.543).

## Declarations (verified against the file)

| Decl | Kind | §  | Status |
|------|------|----|--------|
| `toposHasRightAdjointImage` | instance | 1.946 | done — `HasRightAdjointImage` (S1_70) via `radjImage` |
| `powObj` | abbrev | 1.94 | done — `Ω^A` |
| `ToposMap` | def | 1.941 | done — predicate for topos morphisms |
| `nameOf` | def | 1.942 | done — `curry(fst ≫ χ_m)` |
| `NamedBy` | def | 1.942 | done — `'A'' factors through F` |
| `membershipMap` | def | 1.94 | done — `eval ∘ ⟨id, term≫F⟩` |
| `interIntersection` | def | 1.94 | done — `InverseImage(membershipMap, true)` |
| `interUnion` | def | 1.949 | done — `image(eval ∘ ⟨snd, fst≫F.arr⟩)` |
| `membershipMap_nameOf` | theorem | 1.94 | **done** — `membershipMap('A'') = χ_{A'}` (β-reduction) |
| `inter_le_named` | theorem | 1.943 pt1 | **done** |
| `curry_fst_membershipMap` | theorem | 1.94 | **done** — η-law (name recovered from membership) |
| `classify_interIntersection` | theorem | 1.94 | **done** — `χ_{∩G} = membershipMap G` |
| `nameOf_interIntersection` | theorem | 1.94 | **done** — `'∩G' = G` |
| `inter_le_singleton_named` | theorem | 1.943 pt2 | **done** — per-singleton bound (NOT the family glb; see integrity note) |
| `topos_has_strict_coterminator` | theorem | 1.944 | **sorry** (gated §1.543) |
| `topos_is_regular` | theorem | 1.945 | **sorry** (gated §1.543) |
| `topos_is_logos` | theorem | 1.946 | **sorry** (gated §1.543) |
| `topos_has_rtc` | theorem | 1.947 | **done body** — closure-assembly via `[HasReflTransClosure]` (glb-existence pinned by hyp, not a sorry) |
| `singletonMap` | def | 1.94(10) | **done** — `curry(χ_{Δ_A})` |
| `singletonMap_monic` | theorem | 1.94(10) | **done** — reuse `singletonMapCat_monic` |
| `IsPointSubobj` / `IsWellPointedPart` / `SolvableTopos` | def/class | 1.94(10) | done |
| `capital_is_solvable` | theorem | 1.94(10) | **sorry** (gated §1.543) |

NOTE the genuine glb `inter_is_glb` of §1.943 pt2 is **not present and cannot be stated** here:
`interIntersection` takes a single global name `1 → [A]` (a singleton family), so there is no
`⋂F`-over-a-subobject-family object to be a glb *of*. The reachable fragment is `inter_le_singleton_named`
(proven). The full family-glb needs the contravariant `Ω^(−)` carrying F's jointly-epic point family to a
jointly-monic family — i.e. the §1.543 capitalization machinery. Likewise `singletonMap_entire` is absent:
the book's "U(A1) is entire" is a *representational* claim about a forgetful `U : 𝒞 → Set` the repo lacks
(see the comment above `singletonMap_monic`); only monicity is available here.

## The 4 remaining sorries — all gated on the §1.543 keystone (re-confirmed this pass)

The single ultimate blocker is `capData_exists` / `capitalization_lemma` (S1_54, `Capitalization.lean`),
still `sorry` (the transfinite A ⊆ A* construction). Triangulated: there is **no**
topos→`HasImages`/`RegularCategory`/`HasCoterminator` instance anywhere in the repo, and the parallel
S1_95 instances `topos_is_effective` / `topos_is_positive` / `topos_has_coequalizers` /
`topos_is_bicartesian` are ALL still `sorry` on the same keystone.

- **`topos_has_strict_coterminator`** (§1.944): the initial object is `⋂∅`. The only non-glb route is
  `0 = pullback of false : 1 → Ω`, then `HasCoterminator.ofStrict` (S1_58) needs a `StrictCoterminator 0`
  witness. Strictness (`every map into 0 is iso`, classically via `A × 0 ≅ 0`) is **not scaffolded** —
  no `false`-map / `A×0≅0` / strict-zero infra exists for a topos (`StrictCoterminator` is used only in
  S1_58), and `false` *is* the classifier of the empty subobject of `1` = the `⋂∅` we are building, so the
  route is circular w.r.t. §1.543.

- **`topos_is_regular`** (§1.945): `RegularCategory` bundles `HasImages` (= the §1.943 family-glb image
  `⋂{B'|f factors through B'}`) + `PullbacksTransferCovers` (the S1_95 exactness fact). Both behind §1.543.

- **`topos_is_logos`** (§1.946): `Logos` (S1_70) extends `RegularCategory` + `HasSubobjectUnions`
  + right adjoint `f##`, plus the lattice bottom (`bottomSub`); binary `∪` and `f##` are themselves
  `⋂F` family-glb's.

- **`capital_is_solvable`** (§1.94(10)): the `A*=0` branch needs the strict coterminator (blocked above)
  and the well-pointed-part lub `A* = ⋃{point subobjects}` (a §1.943 family glb). `_hcap : Capital` does
  not supply these.

## Closed in earlier passes (banked)

- `membershipMap_nameOf` (β), `inter_le_named` (§1.943 pt1), `curry_fst_membershipMap` (η),
  `classify_interIntersection`, `nameOf_interIntersection`, `inter_le_singleton_named` (the honest
  reachable fragment of §1.943 pt2), `singletonMap` / `singletonMap_monic`, and the `topos_has_rtc`
  closure-assembly body — all `sorry`-free in their own right (modulo the file-wide `topos_has_exponentials`
  leak below).

## Note on `sorryAx` in closed proofs

`lean_verify` reports `sorryAx` for `inter_le_named` / `membershipMap_nameOf` /
`singletonMap`, but **not** because of these proofs — they contain no `sorry`.
The axiom is inherited transitively through `topos_has_exponentials` (S1_92:39),
which is the topos→`HasExponentials` instance and is itself a `sorry` stub. Any
S1_94 declaration mentioning `powObj`/`curry`/`eval_exp` carries that axiom. The
proofs here are logically complete given the exponentials instance; closing
S1_92's instance would clear the axiom with no change to S1_94.

## §1.948 (remark, no formal statement)

G-sets example shows ∩F can be ∅ even when the family of named subobjects
is non-empty (the forgetful U : YG → Y witnesses this via U(∩F) = ∩(UF) = ∅).
