# §1.26 Tracker — Slice Category A/B

## Status: COMPLETE (core + §1.263); §1.261–1.262 MISSING (informal examples)

### §1.26 — Slice category A/B

| Item              | Declaration          | Status  | Notes                                  |
|-------------------|----------------------|---------|----------------------------------------|
| Objects of A/B    | `Over B`             | DONE    | pair `⟨dom, hom : dom → B⟩`          |
| Morphisms of A/B  | `OverHom X Y`        | DONE    | commuting triangle, field `.f` + `.w` |
| Composition       | `OverHom.comp` / `⊚` | DONE    | diagram-order, explicit                |
| Category axioms   | `overCat`            | DONE    | `Cat` instance for `Over B`           |
| Mono in A/B       | `OverMono`           | DONE    | `@Mono (Over B) _ Z Y m`             |
| Iso in A/B        | `OverIso`            | DONE    | `@IsIso (Over B) _ X Y m`            |
| Iso ↔ inv exists  | `overIso_iff`        | DONE    | unfolds `IsIso`                        |
| Iso → underlying  | `overIso_underlying` | DONE    | `OverIso m → IsIso m.f`              |
| Underlying → iso  | `overIso_of_underlying` | DONE | `IsIso m.f → OverIso m`             |

### §1.261 — B-indexed families / augmented rings

MISSING. This paragraph discusses:
- The equivalence `Set/B ≃ Set^B` (B-indexed families of disjoint sets).
- The category of augmented rings `Ring/ℤ ≅ Ring_without_unit`.

Both require building `Set`, `Ring`, and equivalence-of-categories infrastructure not
present in this repo. The isomorphism `Ring/ℤ ≅ Ring_without_unit` is cited as forward
reference [1.384].

### §1.262 — Local homeomorphisms / Lazard sheaves

MISSING. This paragraph defines:
- The category `LH` of topological spaces + local homeomorphisms.
- `X/Y` in `LH` = Lazard sheaves `Sh(Y)`.

Requires topology (open sets, homeomorphisms). Not formalizable without that
infrastructure. No attempt made.

### §1.263 — Counter-slice B\A; pointed sets

| Item                | Declaration    | Status | Notes                              |
|---------------------|----------------|--------|------------------------------------|
| Objects of B\A      | `Under B`      | DONE   | pair `⟨cod, hom : B → cod⟩`      |
| Morphisms of B\A    | `UnderHom X Y` | DONE   | co-triangle, `.f` + `.w`          |
| Composition         | `UnderHom.comp` / `⊛` | DONE | diagram-order              |
| Category axioms     | `underCat`     | DONE   | `Cat` instance for `Under B`      |
| Mono in B\A         | `UnderMono`    | DONE   | `@Mono (Under B) _ Z Y m`        |

The concrete specializations (pointed sets = `1\Set`, pointed spaces = `1\Top`) are
informal examples requiring concrete categories; not formalized.

## Sorries: NONE
