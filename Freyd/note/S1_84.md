# S1_84.lean — §1.84 Tracker

Freyd & Scedrov, *Categories, Allegories* §1.84: Grothendieck topoi (Giraud definition).

| §     | Term / Decl                             | Kind         | Status  | Note |
|-------|-----------------------------------------|--------------|---------|------|
| 1.84  | `GrothendieckTopos`                     | class        | DONE    | Giraud definition: EffectiveRegular + HasAllCoproducts + HasCoequalizers + LocallyComplete + SMALL basis (gen_idx:Type v, gen_obj, gen_basis:IsBasis) + DisjointCoproduct + PullbacksPreserveArbitraryUnions.  `gen_set`/`has_gen_set` now derived from the small basis. |
| 1.84  | `DisjointCoproduct`                     | structure    | DONE    | Each injection monic, jointly a cover, pairwise disjoint pullbacks |
| 1.84  | `PullbacksPreserveArbitraryUnions`      | def          | DONE    | Inverse-image commutes with arbitrary suprema of subobjects |
| 1.841 | (presheaf examples satisfy Giraud)      | —            | MISSING | Cannot state without formalizing presheaf topos YA and sheaf topos B☞(Y) |
| 1.842 | `graphing_functor_has_right_adjoint`    | theorem      | MISSING | Requires `Cat` instance for Rel(E) and `Functor` instance for E → Rel(E); neither exists yet |
| 1.843 | `subTrace` / `le_of_subTrace_le`        | def/theorem  | DONE    | Trace `Sub(B) ↪ Π_{i:gen_idx}(gen_obj i ⟶ B)→Prop`; basis detects subobjects (mutual ≤) |
| 1.843 | `subobjectIso_of_le_le`                 | theorem      | DONE    | Antisymmetry of the subobject order ⟹ `SubobjectIso` |
| 1.843 | `grothendieck_topos_well_powered`       | instance     | DONE    | `WellPowered E` PROVED.  Index `Sub(B)` by its Type-v trace; pick a representative per trace (Classical.choice); equal traces force a subobject iso.  Axioms: propext, Classical.choice, Quot.sound. |
| 1.843 | `WellCopowered` (class)                 | class        | DONE    | New class: isomorphism types of covers A↠Q bounded by a type in universe v |
| 1.843 | `kpSub` / `coverIso_of_kpSub_iso`       | def/theorem  | DONE    | Kernel-pair subobject of a map; iso kernel-pair subobjects ⟹ `CoverIso` (via §1.566 `covers_same_kernelPair_iso`) |
| 1.843 | `grothendieck_topos_well_copowered`     | instance     | DONE    | `WellCopowered E` PROVED.  Index covers by the Type-v trace of their kernel-pair subobject in A×A; pick a representative cover per trace; bridge gives `CoverIso`.  Axioms: propext, Classical.choice, Quot.sound. |
| 1.844 | `grothendieck_topos_locally_complete`   | instance     | DONE    | Trivially `inferInstance` (LocallyComplete is a field of GrothendieckTopos) |
| 1.845 | `ExtensiveCoproduct`                    | structure    | DONE    | Freyd's extensivity (disjoint + universal coproduct): `inj_monic` (each uⱼ monic) + `support` (a map g:T→ΣSⱼ whose Σf-image lands in injection j₀ is, after a cover, supported on summand Sⱼ₀). This is "pullback of an injection along Σf recovers the summand; off-diagonal pieces vanish", the arbitrary-`I` form Freyd works with in an extensive/pretopos context. |
| 1.845 | `coproduct_is_coproduct_in_Rel`         | theorem      | DONE    | Both directions PROVED (no sorry).  Takes the explicit hypothesis `hext : ExtensiveCoproduct cp`.  U = image(big span m = pair(cpR.desc(Rᵢ.colA≫uᵢ), cpR.desc(Rᵢ.colB))).  ≥ direction: explicit RelHom via cpR.inj i ≫ image.lift m.  ≤ direction: `relLe_of_cover_factor` with cover `c ≫ pb1.π₂ ≫ image.lift span_c`, where `pb1 = pullback(image.lift m, pb_c.π₂)` exposes `q:·→cpR.obj` with `q≫colA_big` factoring through uᵢ; `hext.support` descends q onto `(R i).src` to give the matching φ.  `hext.inj_monic i` cancels uᵢ on the colA leg.  Axioms: propext, Classical.choice. |
| 1.846 | `coequalizer_is_coequalizer_in_Rel`     | theorem      | DONE    | Both ≡-directions + uniqueness PROVED (no sorry; axioms propext, Classical.choice). R' = h°R. Uses the §1.77 reflexive-transitive closure `rtc` (`[HasReflTransClosure E]`) instead of hand-built ⋃ₙSⁿ. **Key move avoiding ∞-distributivity**: with S = f°g∪1∪g°f and the §1.78 self-quotient R/R (`qRR : RelQuot R R`), each piece P of S satisfies P⊚R⊑R (using fR≡gR + simplicity f°f⊑1), so S⊑R/R by maximality of R/R; R/R is reflexive+transitive, so `rtc_minimal` gives rtc S⊑R/R, hence (rtc S)⊚R⊑R. Existence-hard: hh°R=(hh°)⊚R⊑(rtc S)⊚R⊑R via the E-standard containment hh°⊑rtc S (`h_hh_le_rtc`, §1.844). Existence-easy: graph h entire (1⊑hh°). Uniqueness: h is a cover (`coeq_map_is_cover` on a HasCoequalizer built from h_univ), giving 1⊑h°h, plus simplicity h°h⊑1. Two genuine §1.78/§1.844 facts are taken as hypotheses: `qRR` (relational division R/R, exists in any topos) and `h_hh_le_rtc` (effectiveness: hh° adds no identifications beyond f,g). |
| 1.847 | (SAFT gives right adjoint)              | —            | MISSING | Follows from §1.845 + §1.846 + WellCopowered + SAFT (S1_82); blocked by Rel(E) as category |

## Discharged this pass (§1.845 COMPLETE)

- **`ExtensiveCoproduct cp`** (new structure): faithful statement of Freyd's extensivity for an
  arbitrary-`I` coproduct — `inj_monic` (each injection monic) + `support` (summand-support after a
  cover).  This is the "pullback of a coproduct injection along any map, summed, recovers the object;
  off-diagonal pullbacks are 0" condition, supplied as an explicit hypothesis to §1.845 exactly as
  Freyd works in an extensive / pretopos context.  Both fields are LOAD-BEARING in the proof.
- **`graph_comp_recip_le_one_monic`** (private helper): graph(u)⊚graph(u)° ≤ graph(id_A) when u monic.
  Proved via `image_min` with diagSub, matching compose's internal pb = HasPullbacks.has u u.
- **`one_le_graph_comp_recip_monic`** (private helper): graph(id_A) ≤ graph(u)⊚graph(u)° when u monic.
  Proved by lifting the cone (id_A, id_A) through pb to get d:A→pb.pt, then d≫image.lift s.
- **`coproduct_is_coproduct_in_Rel` ≥ direction**: Rᵢ ≤ graph(uᵢ)⊚U proved via explicit RelHom:
  U = image of big span m = pair(cpR.desc(Rᵢ.colA≫uᵢ), cpR.desc(Rᵢ.colB)); the witness
  uses cpR.inj i ≫ image.lift m lifted through the pullback of uᵢ against U.colA.
- **`coproduct_is_coproduct_in_Rel` ≤ direction — NOW DISCHARGED** with `hext : ExtensiveCoproduct cp`.
  `relLe_of_cover_factor` is fed the composite cover `cov = c ≫ pb1.π₂ ≫ image.lift span_c` into
  `(graph uᵢ ⊚ U).src`, where `pb_c = pullback(uᵢ, U.colA)`, `span_c = pair(pb_c.π₁≫id, pb_c.π₂≫U.colB)`,
  and `pb1 = pullback(image.lift m, pb_c.π₂)`.  On `pb1.pt`, `q = pb1.π₁ : ·→cpR.obj` satisfies
  `q≫colA_big = (pb1.π₂≫pb_c.π₁)≫uᵢ` (pullback squares + `image.lift_fac`), i.e. its Σf-image lands in
  injection i.  `hext.support` returns a cover `c:T'↠·` and `φ:T'→(R i).src` with `c≫q = φ≫cpR.inj i`;
  `hext.inj_monic i` cancels uᵢ to match the colA leg, the colB leg is direct.  Axioms: propext,
  Classical.choice (no sorryAx).

## Discharged previous pass (§1.843)

- **well-powered + well-copowered are now fully proved** (zero sorry, only standard axioms).
  Two design points made them go through:
  1. The Giraud definition was strengthened to carry a **small basis**: `gen_idx : Type v`,
     `gen_obj : gen_idx → E`, `gen_basis : IsBasis (fun X => ∃ i, gen_obj i = X)`.  This is
     *more* faithful to the book ("a small generating set", and ℱ is a basis in a pre-topos)
     and is exactly the smallness data the bound needs.  `gen_set`/`has_gen_set` are now
     derived, so nothing downstream broke.
  2. The bound is indexed by a **Type-v trace**, NOT by `Sub(B)` directly.  `Sub(B)` lives in
     `Type u` (it carries a `dom : E`), so it cannot index a `Type v` smallness witness.  The
     trace `Π_{i:gen_idx} (gen_obj i ⟶ B) → Prop` lives in `Type v` (gen_idx : Type v, hom :
     Type v).  Well-copowered indexes by the trace of the kernel-pair subobject in A×A.

## Blockers (remaining)

- **Rel(E) as a category**: §1.842 and §1.847 still require `Rel(E)` to have a `Cat` instance.
  `BinRel E A B` types relations between two fixed objects; the full `Rel(E)` category
  (objects = objects of E, morphisms = BinRel up to ≡) is not yet formalized. §1.847 (SAFT
  right adjoint) remains blocked on this.  §1.845/§1.846 do NOT need it: they are stated and
  proved directly in terms of `BinRel`/`RelLe`/`⊚`.

- **§1.846 — DISCHARGED (∞-distributivity sidestepped).** The earlier blocker — needing the
  countable union ⋃ₙSⁿ as a `BinRel` plus ∞-distributivity (⋃ₙSⁿ)⊚R = ⋃ₙ(Sⁿ⊚R) — is gone.
  The §1.77 `rtc` (reflexive-transitive closure, `[HasReflTransClosure E]`) plus the §1.78
  self-quotient `R/R` (`RelQuot R R`) replace it: instead of distributing ⊚ over the closure
  union, push the closure *below* R/R by minimality (`rtc_minimal`), so that (rtc S)⊚R⊑R falls
  out of `qRR.le` with no union-distributivity at all.  Two genuine topos facts are taken as
  hypotheses rather than re-derived here: `qRR : RelQuot R R` (relational division, complete
  Heyting subobject lattices) and `h_hh_le_rtc` (the §1.844 effectiveness containment
  hh° ⊑ rtc(f°g∪1∪g°f)).  Both are honest, load-bearing, and provable in any Grothendieck topos;
  factoring them out keeps S1_84 from re-developing §1.78 division and §1.844 effectiveness inline.
