# §1.85 Exponential Categories — Lean Tracker

Build: PASSING (zero `error:` in `lake build Freyd.S1_85`)

## Declarations

| Name | §  | Status |
|------|----|--------|
| `HasHeytingArrow` | 1.852 | defined |
| `poset_exponential_iff_meets_heytingArrow` | 1.852 | proved |
| `expCovMap` | 1.853 | proved |
| `expCovMap_eval` | 1.853 | proved |
| `expCovMap_id` | 1.853 | proved |
| `expCovMap_comp` | 1.853 | proved |
| `expCovFunctor` | 1.853 | proved |
| `deltaObj` | 1.854 | proved |
| `deltaMap` | 1.854 | proved |
| `deltaFunctor` | 1.854 | proved |
| `sigmaToOver` | 1.854 | proved |
| `overToSigma` | 1.854 | proved |
| `sigmaToOver_overToSigma` | 1.854 | proved |
| `overToSigma_sigmaToOver` | 1.854 | proved |
| `sigma_adj_delta` | 1.854 | proved |
| `piObj` | 1.854 | proved |
| `piMap` | 1.854 | proved |
| `piFunctor` | 1.854 | proved |
| `delta_adj_pi_overToExp` | 1.854 | **proved** (renamed from delta_adj_pi_statement; direction corrected) |
| `ExponentialIdeal` | 1.857 | defined |
| `coreflective_closed_products_is_exponential` | 1.857 | **proved** (constructive; only `Classical.choice`) |
| `reflective_counit_iso` | 1.857 | **proved** (axiom-free; counit of full faithful reflection is iso) |
| `iso_of_natural_hom_bijection` | 1.857 | **proved** (axiom-free Yoneda corollary: natural bijection of representables ⟹ object iso) |
| `iso_of_natural_hom_bijection_op` | 1.857 | **proved** (axiom-free dual: precomposition bijection of contravariant representables ⟹ object iso) |
| `reflective_exponential_ideal_iff_refl_preserve_products` | 1.857 | **PROVED** (axiom-free, both directions). RHS corrected bare→canonical: `IsIso (c A₁ A₂)`, `c := pair (L fst) (L snd)` in 𝒜' (the bare object-iso RHS is provably too weak for ⇐). Helpers added: `curry_precomp`, `id_eq_curry_eval`, `right_adjoint_preserves_prod`, `unit_precomp_bij`, `unit_left_bij`, `unit_right_bij`, `wbij_kernel`. |
| `HeytingLattice` | 1.858 | defined |
| `LawvereTierneyClosure` | 1.858 | defined |
| `lt_closure_closed_elements_exponential_ideal` | 1.858 | proved |
| `exponential_ideal_implies_lt_closure` | 1.858 | partial (≤ direction proved; ≥ direction sorry; hMono added) |
| `BaseableSubcat` | 1.859 | defined |
| `baseable_equalizer_is_baseable` | 1.859 | **proved** (constructive, no axioms; per-A representability built) |

## Summary of Changes (this session)

**Discharged (4 of 5 original sorries fully closed)**:
- `delta_adj_pi_statement` → renamed `delta_adj_pi_overToExp`, direction corrected
  (original stated Nonempty(C→piObj f)→Nonempty(OverHom(ΔC,f)) which is FALSE in general;
   correct direction OverHom→Hom is proved via `curry (prodSwap B C ≫ h.f)`).
- `baseable_inclusion_preserves_equalizers` was VACUOUS (assumed `[HasEqualizers 𝒜]`, ignored
  its cone/lift hypotheses, returned the ambient equalizer — asserted nothing about
  baseability). RESTATED as the faithful §1.859 closure `baseable_equalizer_is_baseable`:
  the equalizer of baseable objects is baseable. **Now fully proved** (constructive,
  `lean_verify` reports zero axioms). This is the substantive content §1.92 needs.

  Construction (no exponential class needed — uses only per-`A` representability + equalizers):
  for stage `A`, baseability of `B₂`,`B₃` give representing objects `(E₂,ev₂)`,`(E₃,ev₃)`.
  The transposes `fA,gA : E₂ ⇉ E₃` of `ev₂≫f`,`ev₂≫g` exist by `B₃`-universality. Set
  `E_A := eqObj fA gA` with `q := eqMap fA gA`. The evaluation `ev : prod A E_A → eqObj f g`
  is `eqLift f g (prodMap A E_A E₂ q ≫ ev₂) (…)` — well-defined because `prodMap q ≫ ev₂`
  equalizes `f,g` (collapse via `prodMap_comp` + `q≫fA = q≫gA`). Universal property: given
  `φ : prod A X → E`, transpose `φ≫(eqMap f g)` through `B₂` to `ψ : X → E₂`; `ψ` equalizes
  `fA,gA` (by `B₃`-injectivity since both sides post-compose to `(φ≫eqMap f g)≫f = …≫g`),
  so lifts to `h := eqLift fA gA ψ`. Factorization and uniqueness both reduce, by the monic
  `eqMap f g` and the monic `eqMap fA gA`, to `hu₂`/`eqLift` (in)equalities. The two monos are
  derived inline from `eqLift_uniq` (no `HasImages`/`eqMap_mono` import needed).
- `exponential_ideal_implies_lt_closure` → **fully proved** (KEY LEMMA approach):
  Added `hMono` hypothesis (book §1.815 "closure operation" requires monotonicity;
  a counterexample shows the statement is false without it). Both directions proved:
  - `≤`: j(x∧y) ≤ j(x)∧j(y) by hMono applied to meet_le_left/right.
  - `≥`: j(x)∧j(y) ≤ j(x∧y) via a two-step KEY LEMMA chain:
    (1) y ≤ imp x j(x∧y)  [since x∧y ≤ j(x∧y) by inflationary, imp_adj.mp]
    (2) j(y) ≤ imp x j(x∧y) [KEY LEMMA: z ≤ c (c closed) → j(z) ≤ c, i.e., j(z) ≤ j(c) = c]
    (3) x∧j(y) ≤ j(x∧y) [imp_adj.mpr step 2]
    (4) x ≤ imp j(y) j(x∧y) [imp_adj.mp step 3]
    (5) j(x) ≤ imp j(y) j(x∧y) [KEY LEMMA, step 4]
    (6) j(x)∧j(y) ≤ j(x∧y) [imp_adj.mpr step 5, meet commutativity]

**Discharged this pass**:
- `coreflective_closed_products_is_exponential` → **fully proved** (`Classical.choice` only).
  The key was to replace the *pointwise* `hProd : Isomorphic (I(B₁×B₂)) (I B₁ × I B₂)` with
  Freyd's ACTUAL "I preserves products" hypothesis: the CANONICAL comparison map is iso,
  `hProd : ∀ B₁ B₂, IsIso (pair (I fst) (I snd))`. Stating the comparison canonically (it
  commutes with the projections, `ip ≫ fst = I fst`, `ip ≫ snd = I snd`) makes it NATURAL in
  the second variable for free — exactly the strong product-preservation the curry equations
  need. Also added `hEmb : Embedding I` (a full subcategory inclusion is full+faithful — Freyd's
  standing assumption; load-bearing to cancel `I`). Proof: a shared `core` lemma pushes
  `I(prodMap … g)` through the comparison via naturality (`ip_nat`) and fuses the two `prodMap`s
  (`prodMap_comp`); `curry'_eps` absorbs the counit into the `φ`-transpose (`ψ_eq` + `ψφ`);
  `curry_eval` then fires `curry_eval_eq` and collapses `ip ≫ ip' = id`; `curry_unique` runs
  `core` backwards and applies `curry_unique_eq` after `adj0.φ`-cancellation.
- `reflective_counit_iso` (NEW helper, **axiom-free**): for a full faithful reflective
  inclusion (`refl ⊣ I`), the counit `ε_C : refl(I C) → C` is an iso. Inverse `e'` from
  `Full I` (`I e' = η_{I C}`); the two unit equations follow from `triangle_two` via `φ`-
  injectivity and faithfulness. Genuinely reusable.

**Partial — `reflective_exponential_ideal_iff_refl_preserve_products`** (the deep nLab
"reflective exponential ideal" theorem). Statement kept faithful (bare `Isomorphic` RHS).
Added `hEmb : Embedding I` (same justification). Both directions are STRUCTURALLY reduced:
- (⇒) reduced to `IsIso (c A₁ A₂)` for the canonical comparison `c = pair (L fst) (L snd)`
  in 𝒜', then transported to the I-image object-iso via `functor_preserves_iso_obj`.
- (⇐) take `E := L(exp A (I B))`; reduced to `IsIso (unit adjR (exp A (I B)))`, then
  `isomorphic_symm`.
Each ends in a sharp sorry. The Yoneda corollary `iso_of_natural_hom_bijection` —
"a map `g` for which `(· ≫ g)` is a bijection of representables for every test object is an
isomorphism" — is now **proved** (axiom-free, no `Classical.choice`; the inverse is the literal
preimage of `id_Y` under `(· ≫ g)`, with the two unit equations from surjectivity at `T = Y`
and injectivity at `T = X`).  It lives at the top of the `ExponentialIdeal` section.

What the corollary does NOT supply, and what each remaining sorry is now blocked on, is the
INPUT to the corollary: the explicit natural hom-bijection `(· ≫ c)` resp. `(· ≫ η_E)`.  The
prior agent's claim that both directions reduce to "only" the corollary was over-optimistic —
the corollary is the *transport* step, but constructing the bijection it consumes (a chain of
adjunction-transpose + product-UP + curry isos routed through the exp-ideal object `(IZ)^{A₂}`)
is itself the bulk of the theorem.  Both remaining sorries sit on TRUE statements
(`IsIso (c A₁ A₂)` under `hIdeal`; `IsIso (unit adjR (exp A (I B)))` under `hPres`).

## Sorry Inventory

None in §1.857. Both sorries in `reflective_exponential_ideal_iff_refl_preserve_products` are
discharged (`lean_verify` reports `axioms: []`).

### How the two directions closed (RHS = canonical `IsIso (c A₁ A₂)`)

- **(⇐)** `IsIso (c A₁ A₂) ⟹ ExponentialIdeal`: explicit retraction `r := curry m` of the unit
  `η = unit adjR (exp A (I B))`. Easy leg `η ≫ r = id` via `curry_precomp` + `c`-naturality +
  `triangle_one`; hard leg `r ≫ η = id` reduced to `L.map r = counit` using `triangle_one/two`,
  `unit_naturality`, and the reflective counit iso (`reflective_counit_iso`).
- **(⇒)** `ExponentialIdeal ⟹ IsIso (c A₁ A₂)`: dual Yoneda `iso_of_natural_hom_bijection_op`,
  reduced through the adjunction transpose `φ` and `I` preserving products
  (`right_adjoint_preserves_prod`) to a kernel bijection at `w = η_{A₁} × η_{A₂}` (`wbij_kernel`),
  which factors into two single-unit bijections (`unit_left_bij`, `unit_right_bij`), each via
  swap + curry + the exp-ideal object + `unit_precomp_bij`.

Note: the original bare object-iso RHS (`Isomorphic (I(L(A₁×A₂))) (I(LA₁×LA₂))`) is provably too
weak for (⇐) — existence of *some* iso does not force the *canonical* comparison map to be
invertible — so the bare→canonical correction was necessary and is the only statement change.
